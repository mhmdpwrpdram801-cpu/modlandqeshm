# دستورالعملِ فول‌استکِ مدلند قشم — برای Claude Code

> کاربر فارسی‌زبان است. **فارسی جواب بده.**
> این فایل هر جلسه خودکار بارگذاری می‌شود، پس کوتاه نگهش دار. جزئیات جای دیگری است.

---

## ۰. چه چیزی را کجا بخوان

| می‌خواهی … | برو سراغِ |
| --- | --- |
| بفهمی این پروژه چیست و تاریخچه‌ی باگ‌هایش | `README-FOR-CLAUDE-CODE.md` — **کامل، قبل از هر تغییر** |
| بازرس را بشناسی یا بررسیِ تازه بنویسی | `auditor/README.md` |
| رباتِ تلگرام را دست بزنی | `bot/README.md` — **بدونِ خواندنش دست نزن** |
| شِمای دیتابیس را بدانی | `auditor/schema.txt` |
| استانداردِ کار و دروازه‌ی تحویل | همین فایل، بخشِ ۳ و ۴ |

این فایل **جایگزینِ** `README-FOR-CLAUDE-CODE.md` نیست؛ لایه‌ی بالای آن است:
آن‌جا می‌گوید این پروژه چه شکلی است، این‌جا می‌گوید کار را چطور باید انجام داد.

---

## ۱. اولین کارِ هر جلسه

```bash
python3 auditor/audit.py panel/index.html -c auditor/audit.config.json
```

باید سبز باشد و کدِ خروجی ۰ بدهد. **الان ۷۷ بررسی** است و چند دقیقه طول می‌کشد —
نصفه رهایش نکن. نتیجه را به کاربر نشان بده.

دو فایده دارد: معلوم می‌شود محیط سالم راه افتاده، و یک نقطه‌ی شروعِ سالم داری تا
بعداً بدانی چه چیزی را خودت شکسته‌ای. **تا سبز نشده سراغِ تغییرِ کد نرو.**

**اگر بالا نیامد:** بررسی‌های ایستا (بخشِ ۱ و ۲) بدونِ مرورگر هم اجرا می‌شوند، ولی
بقیه به Playwright نیاز دارند. دو گیرِ رایج در جلسه‌های وب:

- `ModuleNotFoundError: playwright` → `pip install playwright`
- `Executable doesn't exist at …` → نسخه‌ی Playwright با کرومیومِ نصب‌شده نمی‌خواند.
  به‌جای دانلودِ دوباره، نسخه‌ای را نصب کن که با همان build جور باشد؛ شماره‌ی build
  در `ls $PLAYWRIGHT_BROWSERS_PATH` دیده می‌شود و در `browsers.json` بسته‌ی Playwright
  هم همان شماره باید باشد.

---

## ۲. پشته — نسخه‌های پایدارِ امروز

<!-- STACK:BEGIN — این بخش خودکار نوشته می‌شود؛ دستی ویرایشش نکن -->
> آخرین به‌روزرسانیِ خودکار: **2026-08-07**

**زمانِ اجرا و پایه**

| کلید | ابزار | نسخه‌ی پایدار | یادداشت |
| --- | --- | --- | --- |
| `node` | Node.js | `24.19.0` | خطِ LTS؛ نسخه‌های فرد پایدار نیستند — پایانِ پشتیبانی 2028-04-30 |
| `deno` | Deno | `2.9.5` | زمانِ اجرای Supabase Edge Functions |
| `python` | Python | `3.14.7` | بازرسِ auditor/audit.py — پایانِ پشتیبانی 2030-10-31 |
| `pg` | PostgreSQL | `18.4` | پایه‌ی Supabase — پایانِ پشتیبانی 2030-11-14 |

**لبه و میزبانی**

| کلید | ابزار | نسخه‌ی پایدار | یادداشت |
| --- | --- | --- | --- |
| `wrangler` | Wrangler | `4.120.0` | در deploy.yml پین شده |
| `workerd` | workerd | `1.20260807.2` | compatibility_date از همین درمی‌آید |
| `wrangler-action` | wrangler-action | `—` | اکشنِ انتشار — ⚠️ گیت‌هاب در این محیط باز نیست؛ در CI پر می‌شود |

**داده و کلاینت**

| کلید | ابزار | نسخه‌ی پایدار | یادداشت |
| --- | --- | --- | --- |
| `supabase-cli` | Supabase CLI | `2.112.0` | مهاجرت و استقرارِ Edge Function |
| `supabase-js` | supabase-js | `2.112.2` | این پنل مستقیم REST می‌زند و از آن استفاده نمی‌کند |

**فرانت (برای پروژه‌های بعدی — این پنل بدونِ build است)**

| کلید | ابزار | نسخه‌ی پایدار | یادداشت |
| --- | --- | --- | --- |
| `typescript` | TypeScript | `7.0.2` |  |
| `vite` | Vite | `8.2.1` | پیش‌فرضِ باندلر |
| `react` | React | `19.2.8` |  |
| `next` | Next.js | `16.3.0` |  |
| `tailwindcss` | Tailwind CSS | `4.3.3` |  |

**تست و CI**

| کلید | ابزار | نسخه‌ی پایدار | یادداشت |
| --- | --- | --- | --- |
| `playwright` | Playwright | `1.62.1` | موتورِ بازرس |
| `vitest` | Vitest | `4.1.10` |  |
| `checkout` | actions/checkout | `—` | در deploy.yml روی v4 — ⚠️ گیت‌هاب در این محیط باز نیست؛ در CI پر می‌شود |

`compatibility_date` معادلِ workerd پایدار: **2026-08-07** — بالا بردنش در `wrangler.toml` یک تغییرِ رفتاری است، نه یک به‌روزرسانیِ ساده.

رانشی بینِ پین‌های مخزن و نسخه‌های بالا دیده نشد.

این جدول توصیه است، نه دستورِ ارتقا. تا وقتی بازرس سبز است و کاربر ایرادی ندارد، عوض‌کردنِ نسخه‌ها به‌خودیِ‌خود ارزش ندارد.
<!-- STACK:END -->

---

## ۳. استانداردها — لایه به لایه

### ۳.۱ فرانت (`panel/index.html`)

- **تک‌فایل و بدونِ build می‌ماند.** فریم‌ورک، باندلر و مرحله‌ی ساخت اضافه نکن.
  جدولِ فرانتِ بالا برای پروژه‌های بعدی است، نه برای این پنل.
- **هیچ منبعِ خارجی.** بدونِ CDN، بدونِ فونتِ گوگل، بدونِ اسکریپتِ شخصِ ثالث.
  هرچه لازم است باید یکی از همان ۱۳ فایلِ `panel/` باشد.
- **راست‌به‌چپ و هر دو تم، همیشه.** رنگِ ثابتِ کدشده (`#fff`، `#000`) ننویس؛
  متغیرِ تم را بردار. بازرس کادرِ روشنِ جامانده در تمِ تیره را می‌گیرد.
- **دسترس‌پذیری که روی گوشی معنا دارد:** هدفِ لمس دستِ‌کم ۴۴×۴۴ پیکسل،
  حالتِ فوکوس دیده شود، پیامِ موفق/خطا در ناحیه‌ی `aria-live` بیفتد،
  نسبتِ کنتراست ۴٫۵:۱ برای متنِ معمولی.
- **`innerHTML` با داده‌ی کاربر ممنوع.** `textContent` بگذار. اگر واقعاً HTML لازم داری،
  خودت بساز و مقادیر را جدا تزریق کن.
- **هر `fetch` سه چیز لازم دارد:** تایم‌اوت (`AbortController`)، مسیرِ خطا که به کاربر
  چیزی بگوید، و **محافظِ پاسخِ کهنه** — درخواستِ دوم که زودتر برگردد نباید نتیجه‌ی
  درخواستِ اول را بنویسد. این‌جا قبلاً باگِ واقعی بوده؛ الگو در کد هست، همان را تکرار کن.
- **قابلیتِ نو را با `@supports` و fallback بردار.** iOS/WebKit عقب‌تر است و کاربر
  آیفون هم دارد. هرچه اضافه کردی، با `-e webkit` هم بسنج.
- **بودجه‌ی کارایی روی اینترنتِ کند + VPN:** LCP ≤ ۲٫۵ ثانیه، INP ≤ ۲۰۰ میلی‌ثانیه،
  CLS ≤ ۰٫۱. چیزی که همیشه لازم نیست، تنبل بار شود (الگویش `h2c.js` است).

### ۳.۲ داده — Supabase / Postgres

- **RLS روی هر جدول روشن.** کلیدِ `anon` عمومی است؛ تنها چیزی که از داده محافظت
  می‌کند سیاستِ RLS است. `service_role` **هرگز** نباید به کلاینت برسد.
- **پرس‌وجوی تازه را با `auditor/schema.txt` مقابله کن.** نامِ ستونِ اشتباه در
  زمانِ اجرا و روی گوشیِ کاربر معلوم می‌شود، نه این‌جا.
- **تغییرِ شِما نسخه‌دار باشد** (مهاجرتِ Supabase)، نه دستی از داشبورد.
- **هیچ `DELETE`/`TRUNCATE`/`DROP` روی دادهٔ واقعی بدونِ توافقِ صریحِ کاربر.**
  در تردید، اول بپرس.
- حذف‌شده‌ها را برنگردان: انبار/موجودی، کد کالا، دسته‌بندی، مبدلِ درهم،
  پیش‌بینی نقدینگی، دستیارِ قیمت‌گذاری، به‌روزرسانیِ گروهیِ قیمت. کاربر عمداً حذفشان کرده.

### ۳.۳ لبه — Cloudflare Worker و Edge Function

- ورکر **کدِ اجرایی ندارد**؛ فقط `panel/` را سرو می‌کند. همین‌طور بماند.
- بالا بردنِ `compatibility_date` یک **تغییرِ رفتاری** است، نه به‌روزرسانیِ ساده.
  جدا از تغییرِ دیگر انجامش بده تا اگر چیزی شکست معلوم باشد از کجاست.
- رباتِ تلگرام (`telegram-bot`): منبعِ حقیقت نسخه‌ی مستقر روی Supabase است و
  `bot/index.ts` کپیِ مرجع. وبهوک باید **idempotent** باشد — تلگرام یک به‌روزرسانی را
  چند بار می‌فرستد و سفارشِ تکراری نتیجه‌ی همین است.
- رمز فقط در Secrets؛ نه در کد، نه در لاگ. در لاگ شماره‌ی تلفن و نامِ مشتری ننویس.

### ۳.۴ انتشار

- **مرج روی `main` یعنی منتشر شد.** گردش‌کار قبلش سه چیز را چک می‌کند (بودنِ هر ۱۳ فایل،
  جلو رفتنِ نسخه‌ی کش، بودنِ کلید) و بعدش وارسی می‌کند که سایتِ زنده واقعاً نسخه‌ی
  تازه را می‌دهد.
- **هر بار که `panel/index.html` عوض شد، نسخه‌ی کش در `panel/sw.js` را جلو ببر**
  (`mlq-YYYY-MM-DD-NN`). بدونِ این، گوشیِ کاربر نسخه‌ی قدیمی را نگه می‌دارد.
- برگشت به عقب = `revert` کردنِ همان کامیت و مرج. دستی چیزی روی سرور نگذار.

---

## ۴. دروازه‌ی تحویل — تا هر هشت‌تا سبز نشده، تحویل نده

1. بازرس سبز، کدِ خروجی ۰:
   `python3 auditor/audit.py panel/index.html -c auditor/audit.config.json`
2. تکرارپذیر، نه شانسی: `… -r 3` — هر بررسی که نتیجه‌ی یکسان نداد باید حل شود.
3. موتورِ سافاری هم: `… -e webkit`
4. **باگی که حل کردی، یک بررسیِ خودکار در بازرس گرفته** که قبل از اصلاح fail شود و
   بعدش pass. این قاعده‌ی مرکزیِ کارِ ماست — هر ۷۷ بررسیِ فعلی از باگِ واقعی درآمده.
5. بررسی‌ای که هشدارِ نادرست می‌دهد، همان‌جا اصلاح یا حذف شده.
6. `panel/index.html` عوض شده ⇒ نسخه‌ی کشِ `panel/sw.js` جلو رفته.
7. با چشم چند تا از عکس‌های `/tmp/audit_shots` را دیده‌ای. سبز بودنِ اعداد شواهدِ
   دیدنِ صفحه نیست.
8. آنچه نتوانستی وارسی کنی را **صریح به کاربر گفته‌ای**. «تست کردم» بدونِ اجرا ننویس.

قاعده‌ی بالاسری: **اول خودت باگ را پیدا کن، بعد تحویل بده.** کاربر صریح گفته
«اول چک کن بعد بده، نه اینکه بدهی و بعد چند دور دیباگ کنیم».

---

## ۵. این سند خودش را به‌روز می‌کند

جدولِ بخشِ ۲ دستی نوشته نمی‌شود. `scripts/update_stack.py` نسخه‌ها را از منابعِ رسمی
می‌گیرد (registry.npmjs.org، endoflife.date، ریلیزهای گیت‌هاب)، جدول را بازنویسی می‌کند،
و پین‌های خودِ مخزن — `compatibility_date` در `wrangler.toml` و `wranglerVersion` در
`deploy.yml` — را با آن‌ها می‌سنجد و رانش را همان‌جا می‌نویسد.

```bash
python3 scripts/update_stack.py            # بگیر و بنویس
python3 scripts/update_stack.py --check    # فقط گزارش، فایل دست‌نخورده
python3 scripts/update_stack.py --offline  # بدونِ شبکه، فقط سنجشِ رانش
```

گردش‌کارِ `.github/workflows/stack-update.yml` هر دوشنبه صبح خودش این را می‌زند و اگر
چیزی عوض شده باشد یک **PR** باز می‌کند. مستقیم روی `main` نمی‌نویسد — عوض شدنِ نسخه‌ها
خبر است، نه دستور؛ آدم باید ببیندش.

اگر منبعی جواب ندهد، مقدارِ قبلیِ همان ردیف می‌ماند و کنارش ⚠️ می‌خورد. یک قطعیِ
شبکه سند را خراب نمی‌کند.

**برای اضافه‌کردنِ ابزارِ تازه فقط `scripts/stack_sources.json` را ویرایش کن.**
اسکریپت را دست نزن.

---

## ۶. دستورهای آماده

| دستور | کار |
| --- | --- |
| `/fullstack` | چک‌لیستِ کاملِ لایه‌به‌لایه برای یک تغییرِ تازه |
| `/ship` | اجرای کاملِ دروازه‌ی تحویلِ بخشِ ۴ و گزارشش |
| `/stack` | به‌روزرسانیِ جدولِ پشته و گزارشِ رانش |

---

## ۷. سه چیزی که هیچ‌وقت نکن

1. تحویلِ نادیده — «باید کار کند» به‌جای «اجرا کردم و این خروجی‌اش است».
2. حذفِ داده یا کارِ مخرب روی دیتابیسِ واقعی، بدونِ توافقِ قبلی.
3. برگرداندنِ قابلیت‌هایی که کاربر عمداً حذف کرده (فهرستشان در ۳.۲).
