# ادامه‌ی کار روی پنلِ مدلند قشم — راهنما برای Claude Code

> این پوشه را کامل بخوان و بعد شروع کن. کاربر فارسی‌زبان است؛ **فارسی جواب بده**.

---

## ۱. این چیست

پنلِ مدیریتِ فروشِ عمده‌ی پوشاک، برای یک کسب‌وکار واقعی در قشم.
مالک با گوشی کار می‌کند (اندروید و آیفون)، اینترنتش کند است و معمولاً VPN دارد.

- **یک فایلِ تک‌پارچه:** `panel/index.html` — HTML + CSS + JS در یک فایل، بدونِ فریم‌ورک،
  بدونِ مرحله‌ی build. حدود ۲۰۹ تابع و ۲۰۰۰ خط جاوااسکریپت.
- **فارسی، راست‌به‌چپ، موبایل‌محور، PWA** با تمِ روشن و تاریک.
- **دیتابیس:** Supabase (Postgres) — پنل مستقیم با REST به آن وصل می‌شود.
- **میزبانی:** یک Cloudflare Worker که پوشه‌ی `panel/` را سرو می‌کند.
- **یک رباتِ تلگرام** هم هست (Supabase Edge Function به نامِ `telegram-bot`) که
  مشتری‌ها با آن سفارش می‌دهند. کدش روی Supabase اجرا می‌شود؛ یک **کپیِ مرجع** هم در
  `bot/index.ts` هست. روالِ ویرایش و استقرارش در `bot/README.md` نوشته شده — قبل از
  دست‌زدن به ربات حتماً آن را بخوان.

## ۱.۵ اولین کاری که باید بکنی

قبل از هر تغییری، **بازرس را یک بار اجرا کن و نتیجه‌اش را به کاربر نشان بده**:

```
python3 auditor/audit.py panel/index.html -c auditor/audit.config.json
```

دو دلیل دارد: اول اینکه معلوم شود محیطت درست راه افتاده (`playwright` و `node` نصب‌اند
و کرومیوم دانلود شده)، دوم اینکه یک نقطه‌ی شروعِ سالم داشته باشی تا بعداً بدانی چه چیزی
را خودت شکسته‌ای. الان باید روی کرومیوم **۱۰۰ بررسی پاس** بدهد و کدِ خروجی ۰ باشد.
(روی وب‌کیت ۹۷ پاس + ۳ هشدار، چون سه بررسیِ کدکِ ویدیو آنجا پشتیبانی نمی‌شود.)

اگر اجرا نشد، همان‌جا با کاربر حلش کن و تا سبز نشده سراغِ تغییرِ کد نرو.

## ۲. فایل‌ها

```
panel/     ← دقیقاً همین ۱۳ فایل روی سرور آپلود می‌شوند
  index.html         خودِ برنامه
  sw.js              Service Worker (کش و به‌روزرسانی)
  h2c.js             کتابخانه‌ی عکس‌گرفتن از فاکتور — تنبل بار می‌شود
  manifest.json
  font1/2.woff2      فونتِ وزیرمتن
  img1..4.png        لوگو و بارکدهای فاکتور
  icon-*.png
auditor/   ← روی سرور نمی‌رود؛ ابزارِ داخلی
  audit.py           بازرسِ خودکار، ۸۳ بررسی
  README.md          فهرستِ بررسی‌ها و قالبِ تنظیمات
  audit.config.json  تنظیماتِ همین پروژه
  fixture.js         شبیه‌سازِ شبکه (تست به سرورِ واقعی نمی‌زند)
  schema.txt         شِمای دیتابیس برای مقابله‌ی پرس‌وجوها
  build_zip.sh       ساختِ بسته‌ی قابلِ آپلود
  server_audit.py    بازرسِ سمتِ سرور — متنِ bot/index.ts را می‌سنجد (بی‌مرورگر)
  mutants.py         تستِ جهشی: باگ می‌کارد و می‌پرسد دروازه گرفتش یا نه
  mutgen.py          جهش‌های ماشینی (بر خلافِ mutants.py که دستی‌اند)
  botsim/            شبیه‌سازِ تلگرام — خودِ ربات را اجرا می‌کند، ۹ بررسیِ رفتاری
    run.ts             بررسی‌ها
    stub_supabase.ts   دیتابیسِ درون‌حافظه‌ای؛ هر چیزِ نشناخته را بلند خطا می‌دهد
    import_map.json    supabase-js را به همان استاب وصل می‌کند
  tests/             تستِ **خودِ بازرس** — ۹ مورد، هرکدام یک نمونه‌ی معیوب و یک سالم
    run.py             اجرا: هم می‌گیرد؟ هم روی سالم ساکت می‌مانَد؟
    fixtures/          نمونه‌های جفتی
bot/       ← روی سرور نمی‌رود و در zip هم نیست؛ کپیِ مرجعِ رباتِ تلگرام
  index.ts           کدِ Edge Function (منبعِ حقیقت نسخه‌ی مستقر است)
  README.md          روالِ امنِ ویرایش و استقرار + دودتستِ بعدش
db/migrations/ ← فایلِ مهاجرتِ شِما. هر تغییرِ دیتابیس اینجا کامیت می‌شود (DATA-01).
expenses/  ← ابزارِ جدا و مستقلِ ثبتِ خرجِ روزانه؛ ربطی به پنل و دیتابیس ندارد
  index.html         خودِ ابزار (تک‌فایل، داده روی localStorageِ همان گوشی)
  audit.config.json  تنظیماتِ بازرس برای همین ابزار (۴۱ بررسیِ ویژه)
  README.md          قاعده‌هایش و درس‌هایی که موقعِ ساختش گران درآمد
importer/  ← **محصول نیست، زمینِ آزمایشِ دستورالعمل است.** روی سرور نمی‌رود.
  index.html         ابزارِ واردکردنِ CSV/JSON؛ دو پارسرِ عمداً جدا (API-07)
  audit.config.json  ۱۸ بررسیِ ویژه روی قاعده‌هایی که هیچ‌جای دیگر نمی‌سنجیدشان
  README.md          چرا هست و سه هشدارِ نادرستی که در خودِ بازرس لو داد
voice/     ← ابزارِ دسکتاپِ گفتار‌به‌متنِ ویندوز (mlqvoice)؛ نه روی سرور می‌رود نه در zip
  README.md          راه‌اندازی، دیکشنری، و فهرستِ صریحِ چیزهایی که تست نشده‌اند
  src/mlqvoice/      پایتون + PySide6. دروازه‌اش pytest و ruff است، نه بازرسِ playwright
wrangler.toml        تنظیماتِ انتشارِ Cloudflare (ورکر assets-only)
.github/workflows/
  deploy.yml         انتشارِ خودکار روی هر تغییرِ panel/ در main
```

### انتشار

**قبلاً گیت فقط سابقه بود و سایت با آپلودِ دستی بالا می‌رفت. دیگر این‌طور نیست.**
مرج روی `main` یعنی منتشر شد. گردش‌کار قبل از انتشار سه چیز را چک می‌کند
(بودنِ هر ۱۳ فایل، جلو رفتنِ نسخه‌ی کش، و درست بودنِ کلید) و بعد از انتشار
تا یک دقیقه صبر می‌کند تا ببیند سایتِ زنده واقعاً نسخه‌ی تازه را می‌دهد.

دو رمز در `Settings → Secrets and variables → Actions` لازم است:
`CLOUDFLARE_API_TOKEN` (اجباری) و `CLOUDFLARE_ACCOUNT_ID` (فقط اگر توکن به بیش از
یک حساب دسترسی دارد).

## ۳. قاعده‌های کار با این کاربر — این‌ها مهم‌اند

1. **قبل از تحویل، خودت باگ را پیدا کن.** کاربر صریح گفته: «اول چک کن بعد بده،
   نه اینکه بدهی و بعد چند دور دیباگ کنیم».
2. **همیشه بازرس را اجرا کن** و فقط وقتی سبز شد تحویل بده:
   ```
   python3 auditor/audit.py panel/index.html -c auditor/audit.config.json
   ```
   کدِ خروجی ۰ یعنی آماده، ۱ یعنی تحویل نده.
3. **هر باگی که پیدا شد، قبل از بسته‌شدن یک بررسیِ خودکار در بازرس بگیرد**
   که قبل از اصلاح fail شود و بعد از اصلاح pass. این قاعده‌ی مرکزیِ کارِ ماست
   و همه‌ی ۷۱ بررسیِ ویژه‌ی پروژه از باگِ واقعی درآمده‌اند — یا باگی که به دستِ مالک
   رسیده، یا باگی که `auditor/mutants.py` عمداً کاشته و دروازه نگرفته بود.
4. **بررسی‌ای که هشدارِ نادرست می‌دهد را همان‌جا اصلاح یا حذف کن.** ابزاری که الکی
   قرمز می‌دهد بعد از چند بار نادیده گرفته می‌شود.
4.۵ **قبل از باور کردنِ سبز، تکرارپذیریش را بسنج:**
   ```
   python3 auditor/audit.py panel/index.html -c auditor/audit.config.json -r 3
   ```
   `--repeat` هر بررسی را چند بار می‌زند و هرکدام که نتیجه‌ی یکسان نداد را **با اسم**
   گزارش می‌کند. `reset` هم قبل از *هر* بررسی اجرا می‌شود، نه فقط آن‌هایی که `before`
   دارند — قبلاً هر بررسی وضعیتِ جامانده از قبلی را به ارث می‌برد و چون ترتیبِ
   مرحله‌ها روی هر موتور فرق می‌کند، نتیجه شانسی می‌شد. **یک بررسی همین‌طور یک روز
   قرمز و روز بعد سبز بود و باعثِ یک تشخیصِ کاملاً غلط شد.** بررسیِ تازه‌ای که نوشتی
   باید خودش شرایطش را بسازد، نه اینکه به وضعیتِ جامانده تکیه کند.
5. **انتشار خودکار است: مرج روی `main` یعنی منتشر شد.** با هر تغییری در `panel/`،
   گردش‌کارِ `.github/workflows/deploy.yml` پوشه را روی Cloudflare می‌گذارد و بعدش
   خودش وارسی می‌کند که سایتِ زنده واقعاً نسخه‌ی تازه را سرو می‌کند.
   می‌شود دستی هم اجرایش کرد (`workflow_dispatch`) بدونِ اینکه چیزی مرج شود.
   `build_zip.sh` هنوز هست ولی فقط برای وقتی که بخواهی دستی آپلود کنی — مسیرِ عادی نیست.
6. **هر بار که `index.html` عوض شد، نسخه‌ی کش در `sw.js` را جلو ببر**
   (`mlq-YYYY-MM-DD-NN`). اگر این کار نشود، گوشیِ کاربر نسخه‌ی قدیمی را نشان می‌دهد.
   **حالا گردش‌کار خودش جلویت را می‌گیرد:** اگر `panel/` عوض شده باشد و نسخه جلو نرفته
   باشد، انتشار با خطا می‌ایستد.
6.۵ **PRِ کارِ خودت را خودت مرج کن — نپرس.** مالک صریح گفته: «بدون اینکه از من
   بخوای بگم مرج کن، خودت مرج کن». پس وقتی CI سبز شد و دروازه‌ی وارسی پاس داد،
   از حالتِ پیش‌نویس درش بیاور و مرج کن.
   **ولی این اجازه فقط برای PRهایی است که خودت در همین کار ساخته‌ای.** PRِ قدیمی
   یا PRی که نخوانده‌ای را مرج نکن؛ اول بخوان و اگر با کارِ فعلی می‌جنگد، بگو.
   دروازه‌ی وارسیِ قرمز هم با هیچ اجازه‌ای مرج نمی‌شود.
7. **بدونِ توافقِ قبلی هیچ داده‌ای حذف نکن** و روی دیتابیسِ واقعی کارِ مخرب نکن.
8. کاربر **انبار/موجودی را عمداً حذف کرده** و نمی‌خواهد برگردد. همچنین: کد کالا،
   دسته‌بندی، مبدلِ درهم، پیش‌بینی نقدینگی، دستیارِ قیمت‌گذاری و به‌روزرسانیِ گروهیِ قیمت
   حذف شده‌اند. برشان نگردان.

## ۴. چطور تست کن — درس‌های گران

این‌ها از باگ‌های واقعی درآمده‌اند که به دستِ کاربر رسیدند:

- **کلیکِ واقعی بزن، تابع را صدا نزن.** یک بار دکمه‌ها زیرِ یک لایه گیر کرده بودند؛
  همه‌ی تست‌هایی که تابع را مستقیم صدا می‌زدند سبز بودند.
- **HTML را جدا از مرورگر اعتبارسنجی کن.** یک `</nav>` جای اشتباه رفت؛ کرومیوم
  خودش ترمیمش کرد و در تست کار می‌کرد، ولی روی گوشیِ کاربر نوارِ پایین کلِ صفحه را
  در خود گرفته بود و هیچ دکمه‌ای کار نمی‌کرد.
- **هر دو تم را اندازه بگیر.** «صفحه در تمِ تاریک باز شد» شواهد نیست؛ چند کادر
  سفید مانده بودند و کسی ندید.
- **عکس بگیر و واقعاً نگاه کن.** چند پنجره پس‌زمینه‌ی شفاف داشتند و محتوای پشتشان
  دیده می‌شد؛ فقط با دیدنِ عکس معلوم شد.
- **موقعِ ویرایشِ برنامه‌ایِ فایل، تعدادِ تطابق را بررسی کن** و درجِ مبتنی بر
  محاسبه‌ی موقعیت نکن مگر با اعتبارسنجیِ بعدش. باگِ `</nav>` دقیقاً از همین راه ساخته شد.
- **پول را با عددِ دستی‌حساب‌شده مقایسه کن**، شامل مقدارِ مرزی.
- **هر صفحه‌ای که در `screens` تنظیمات نباشد، برای بازرس وجود ندارد.** دو باگ دقیقاً
  همان‌جا پنهان بودند: صفحه‌ی «فرمِ مرجوعی» در فهرست نبود و دو کارتش در تمِ تیره
  کنتراستِ ۱٫۰۹ داشتند (عملاً نامرئی)، یکی‌شان هم مبلغِ برگشتی به مشتری بود.
  هر صفحه‌ی تازه که ساختی، همان موقع به `screens` اضافه‌اش کن.
- **با نامِ بلند و بدقواره تست کن، نه فقط نامِ تمیز.** شبیه‌ساز عمداً یک کالا با نامِ
  بلندِ پر از گیومه و `<b>` و `&` دارد. با نامِ کوتاه همه‌چیز سالم به نظر می‌رسید، ولی
  همان نام در ۳۲۰ پیکسل ستونِ «قیمت کل» را از کادرِ فاکتور بیرون می‌زد — و چون
  `.invoice` مقدارِ `overflow:hidden` دارد، عدد **بریده** می‌شد نه اینکه بشود کشیدش.
- **قبل از باور کردنِ یک دسته خطا، اول بپرس بازرس اصلاً وارد برنامه شده یا نه.**
  یک بار بازرس ۱۷ «باگ» داد — هشت دکمه‌ی timeout‌شده و نُه بررسیِ صفر — و همان کد
  چند دقیقه قبل و بعدش کاملاً سبز بود. هیچ باگی در کار نبود: مرحله‌ی ورود نیمه‌کاره
  مانده بود، `#app` مخفی مانده بود و همه‌چیز روی صفحه‌ی ورود اجرا می‌شد. حالا
  `login_done` در تنظیمات هست و بازرس تا واقعاً آماده نشدنِ برنامه جلو نمی‌رود؛
  اگر نشد یک خطای روشن می‌دهد به‌جای ۱۷ خطای جعلی. **قاعده: خرابیِ خوشه‌ای که همه‌ی
  بررسی‌های محتوایی را با هم می‌خواباند، تقریباً همیشه ایرادِ ابزار است نه برنامه.**
- **به شبیه‌ساز اعتماد نکن، اولش خودش را وارسی کن.** `fixture.js` فیلترِ
  `invoice_id=is.null` را اجرا نمی‌کرد و فیلترِ جدولِ تودرتو را هم اشتباه اعمال می‌کرد،
  یعنی صفحه‌ها را روی حالتی نشان می‌داد که در دیتابیسِ واقعی ممکن نیست. یک بار همین
  باعث شد یک «باگِ پول» گزارش شود که اصلاً وجود نداشت. وقتی عددی مشکوک بود، **اول
  پرس‌وجوی واقعی و کدِ خودِ دیتابیس را بخوان**، بعد قضاوت کن.
- **بررسیِ هندسه یا لمس بعد از `nav()` باید دستِ‌کم ۶۰۰ میلی‌ثانیه صبر کند.** تا وقتی
  گذارِ نما (View Transition) در جریان است، مرورگر یک عکسِ ثابت نشان می‌دهد و
  `elementFromPoint` روی DOMِ زنده نمی‌افتد — `html` برمی‌گردانَد، انگار چیزی آنجا
  نیست. با ۸ تکرار در هر تأخیر اندازه گرفته شد: ۰ms درست، ۵۰ و ۱۵۰ms هر هشت بار
  `html`، ۳۰۰ms نصف‌نصف، ۶۰۰ و ۹۰۰ms هر هشت بار درست. `getBoundingClientRect` در
  همین مدت مقدارِ درست می‌دهد، پس بررسی «بی‌ثبات» می‌شود نه یک‌دست قرمز — و آدم را
  دنبالِ باگی می‌فرستد که وجود ندارد.
- **بررسی‌ای که می‌گوید «لمس دزدیده شد» باید بگوید دزد که بود.** اولین نسخه‌ی همین
  بررسی فقط می‌گفت شکست خورد؛ با یک خط اضافه که تگ و شناسه‌ی عنصرِ رویی را برمی‌گرداند،
  در همان اجرای بعدی معلوم شد `html` است و مسیر یک‌راست به گذارِ نما رسید. **پیامِ
  شکست را طوری بنویس که خودش قدمِ بعدی را نشان بدهد.**

## ۵. نکته‌های فنیِ لازم

- **پول:** تومان، عددِ صحیح، بدونِ اعشار. ورودی با `parseNum` خوانده می‌شود
  (جداکننده‌ی هزار و ارقامِ فارسی را می‌فهمد). برای اعشار `faFloat` هست — **برای پول
  از آن استفاده نکن**، یک بار همین باگ ساخت.
- **تاریخ:** جلالی، به‌صورت رشته‌ی `۱۴۰۵/۰۵/۰۹`. ستونِ `date` خودش جلالی ذخیره شده و
  مستقیم چاپ می‌شود، ولی `created_at` **میلادیِ ISO** است — آن را با `jDate()` نشان بده.
  `slice(0,10)` روی `created_at` یعنی «2026-08-06» کنارِ «۱۴۰۵/۰۵» و یک بار همین اتفاق
  در سه صفحه افتاده بود. **هیچ‌وقت `created_at` را خام چاپ نکن.**
- **هر کادرِ ورودی `maxlength` دارد، جز رمز عبور.** قبلاً هیچ‌کدام نداشتند و می‌شد
  بی‌نهایت نویسه تویشان ریخت. سقف‌ها بر اساسِ کارِ هر کادر است (نام ۶۰، شهر ۴۰،
  توضیحات ۲۰۰، اندازه و قواره ۵۰۰، پول ۱۳، شماره ۱۸، …).
  **دو دام:**
  ۱. **سقفِ کادرِ پول بر اساسِ شکلِ نهایی است، نه تعدادِ رقم** — جداکننده‌ی هزار هم
     شمرده می‌شود. اندازه‌گیری شد: با `maxlength=11` فقط ۹ رقم جا می‌شود
     (`۹۹۹٬۹۹۹٬۹۹۹`). پس ۱۳ یعنی ۱۰ رقم، نه ۱۳ رقم. همین برای شماره هم صادق است
     (فاصله‌های گروه‌بندی). **اگر سقفی را کم کردی، اول بشمار چند رقم واقعاً می‌مانَد.**
  ۲. **`maxlength` مقدارِ برنامه‌ای را نمی‌بُرد** — سنجیده شد: نشاندنِ ۳۰۰ نویسه با
     کد کامل می‌مانَد. یعنی بارگذاریِ داده‌ی موجود از دیتابیس بریده نمی‌شود و این
     تغییر هیچ داده‌ای را خراب نمی‌کند. فقط جلوی تایپ و چسباندن را می‌گیرد.
  **روی `lPass` عمداً سقف نیست** — اگر رمزِ مالک از سقف بلندتر باشد، دیگر نمی‌تواند
  وارد شود. **این را «جا افتاده» حساب نکن و سقف نگذار.**
  ستون‌های دیتابیس همه `text` بدونِ سقف‌اند، پس این‌ها فقط محافظِ ورودی‌اند نه قیدِ
  داده. بررسیِ خودکارش هر کادرِ بی‌سقف را **با اسم** گزارش می‌کند.
- **شماره تماس: `canonicalPhone` ذخیره می‌کند، `phoneFmt` نشان می‌دهد.** مالک خواست
  شماره‌ها مثلِ واتساپ گروه بخورند (`۰۹۱۷ ۲۱۲ ۸۷۰۸`). این **فقط جلوه است** — در
  دیتابیس همیشه فقط رقم می‌نشیند. هر پنج مسیرِ ذخیره (`nPhone` در فاکتور و
  پیش‌فاکتور، `cPhone` در مشتری، `sPhone`/`sPhone2` در تنظیمات) از `canonicalPhone`
  رد می‌شوند. **اگر ورودیِ تازه‌ای ساختی، هم `oninput="phoneInput(this)"` بده هم موقعِ
  ذخیره از `canonicalPhone` ردش کن** — یکی بدونِ دیگری یعنی فاصله در دیتابیس.
  (`sPhone` دقیقاً همین‌طور بود: `.value.trim()` ذخیره می‌کرد و تا وصل کردنِ
  گروه‌بندی، فاصله‌ها می‌رفتند توی `settings`.)
  دو قاعده‌ی خودِ `phoneFmt`: **هیچ رقمی نمی‌خورد** (شماره‌ی بلندتر از قالب، باقی‌اش
  ته رشته می‌آید) و **شکلِ ناشناخته را حدس نمی‌زند** (شماره‌ی خارجی مثلِ `+971…`
  دست‌نخورده می‌ماند). `canonicalPhone` و `waLink` هر دو خودشان غیرِ‌رقم را دور
  می‌ریزند، پس فاصله به ذخیره و به نشانیِ wa.me آسیب نمی‌زند.
  **روی سند هر دو شماره گروه می‌خورند** — خریدار و خودِ فروشگاه. اولین بار فقط
  شماره‌ی خریدار گروه خورد و ته همان فاکتور شماره‌ی فروشگاه خام ماند؛ در عکس دیده
  شد و بررسیِ خودکارش حالا «سندِ دوشکل» را می‌شمارد. **دو بررسی دارد.**
- **⚠️ شماره‌ی گروه‌خورده در متنِ راست‌به‌چپ وارونه دیده می‌شود — از `phoneTag()` رد کن،
  نه `esc(phoneFmt())`.** این باگ به دستِ مالک رسید و مالِ خودِ همین گروه‌بندی بود:
  تا وقتی شماره یک رشته‌ی پیوسته‌ی رقم بود، الگوریتمِ دوسویه دست‌نخورده نگهش می‌داشت.
  با فاصله‌ها، هر گروه یک «عددِ اروپایی» جدا می‌شود و در بندِ راست‌به‌چپ **ترتیبِ
  دیداریشان برعکس** می‌شود. اندازه‌گیری شد: منطقی `0900 000 0000` روی صفحه
  `0000 000 0900` خوانده می‌شد — یعنی مالک شماره را وارونه می‌خواند و به آدمِ اشتباه
  زنگ می‌زد. سندِ فاکتور سالم بود چون از قبل `dir="ltr"` داشت؛ لیستِ مشتری‌ها،
  صفحه‌ی مشتری، جست‌وجو، کارتِ مشتریِ غیرفعال و صورت‌حساب نداشتند.
  `phoneTag()` شماره را در `<bdi dir="ltr">` می‌بندد. تنها استثنا `value=` ورودی‌هاست
  که تگ نمی‌پذیرد و خودشان `dir="ltr"` دارند.
  **درسِ روشی:** بررسیِ رشته‌ای این را **هرگز** نمی‌گرفت — رشته درست بود، چیدمانِ
  دیداری غلط. بررسیِ تازه ترتیب را از `getBoundingClientRect` هر گروه می‌خوانَد،
  نه از `innerText`. **هر جا چیدمانِ راست‌به‌چپ مهم است، مختصات را اندازه بگیر نه متن را.**
- **قیمتِ واحد در فاکتور هیچ فیلدی ندارد** (خواستِ صریحِ کاربر — نه ورودی، نه فیلدِ
  پنهان). هرجا لازم شد با `rowUnitPrice(row)` مستقیم از `PRODUCTS` خوانده می‌شود.
  زیرِ هر ردیف فقط «۳ عدد × ۸۵۰٬۰۰۰ تومان» نشان داده می‌شود. **فیلد را برنگردان.**
- **استثنای پیش‌فاکتور:** قیمتی که به مشتری قول داده شده روی خودِ ردیف قفل می‌شود با
  `data-lock` (باز هم فیلد نیست، یک نشانه روی `div` است). `rowUnitPrice` اول قفل را
  می‌بیند و بعد `PRODUCTS`. قفل با عوض‌کردنِ کالا خودکار باز می‌شود، و اگر قیمتِ روز
  فرق داشته باشد کنارِ ردیف دکمه‌ی «قیمتِ امروز … بشه؟» می‌آید. یعنی گران‌شدنِ کالا
  پیش‌فاکتورهای داده‌شده را به‌هم نمی‌زند، ولی اگر خواستی خودت یک‌کلیک به‌روزش می‌کنی.
- **پیش‌فاکتور و فاکتور یک سازنده دارند: `buildInvoiceHTML(d, {doc:'quote'})`.**
  وسوسه می‌شوی برای پیش‌فاکتور یک قالبِ جدا بنویسی — ننویس. دو نسخه‌ی موازی بالاخره
  از هم جدا می‌افتند و آن‌وقت سندی که دستِ مشتری می‌رسد با فاکتور فرق دارد، بی‌آنکه
  کسی بفهمد. فرقِ حالتِ پیش‌فاکتور فقط چهار چیز است و همه‌شان با همان یک پرچم:
  عنوان، **نداشتنِ شماره** (پیش‌فاکتور شماره ندارد)، **نداشتنِ «پرداخت‌شده / مانده
  حساب»** (هنوز پولی رد و بدل نشده و نوشتنش دروغ است)، و کادرِ `.inv-note` که صریح
  می‌گوید این سندِ فروش نیست. `quoteDocData(q)` ردیفِ `quotes` را به همان شکل درمی‌آورد
  و **`created_at` را از `jDate()` رد می‌کند** — خام چاپش نکن.
  قیمتِ واحد از خودِ ردیفِ ذخیره‌شده خوانده می‌شود نه از `PRODUCTS`، چون همان قیمتی
  است که قول داده شده. **دو بررسیِ خودکار دارد** (شکلِ سند + کنتراست).
- **رنگِ متنِ `.inv-note` عمداً `--gold-dp` است، نه `--gold-lt`.** با `gold-lt` کنتراست
  روی پس‌زمینه‌ی `--panel` می‌شود ۴٫۱۸ و روی نشانِ نیمه‌شفاف ۳٫۹۹ — هر دو زیرِ ۴٫۵. متنِ
  ۱۲٫۵ پیکسلیِ توپُر «درشت» حساب نمی‌شود که سقفش پایین‌تر باشد. **موقعِ سنجشِ کنتراستِ
  پس‌زمینه‌ی نیمه‌شفاف، حتماً آلفا را با لایه‌ی زیرش ترکیب کن** — اولین اندازه‌گیریِ من
  `rgba(...,.13)` را مثلِ رنگِ توپُر حساب کرد و عددِ ۱٫۵۶ داد که کاملاً غلط بود.
- **خرج در گزارش نیست.** جدولِ `expenses` روی دیتابیس هست و در پشتیبان هم می‌آید، ولی
  پنل هیچ‌جا فرمِ ثبتِ خرج ندارد. پس «گزارشِ فروش» هم عمداً ردیفِ خرج را نشان نمی‌دهد؛
  عددی که همیشه صفر است فقط گمراه می‌کند. **اگر روزی فرمِ ثبتِ خرج ساختی، آن‌وقت ردیف را
  برگردان — نه قبلش.** بررسیِ خودکارش در بازرس هست.
  **دامِ تازه (۱۴۰۵/۰۵/۲۲):** حالا `expenses/index.html` هست و خرج ثبت می‌کند، ولی
  **روی `localStorage`ِ گوشی، نه در جدولِ `expenses`**. یعنی این شرط هنوز برآورده نشده و
  ردیفِ خرج در گزارشِ پنل **همچنان نباید برگردد** — عدد در دیتابیس نیست که خوانده شود.
- **مبلغ‌به‌حروف فقط دو جا می‌آید: فاکتور و پیش‌فاکتور.** آن‌جا سند است و جلوی جعل را
  می‌گیرد؛ در داشبورد و گزارش و لیست‌ها فقط ارتفاعِ هر کارت را دو برابر می‌کرد تا همان
  عددِ بالا را دوباره بگوید. **به لیست‌ها برش نگردان.** استثنا: راهنمای زیرِ فیلدهایی که
  داری عدد تویشان تایپ می‌کنی (`pPriceWords`، `payWords`، `salWords`، `tFinalWords`،
  `invRetWords`) — آن‌ها برای گرفتنِ اشتباهِ صفرِ اضافه‌اند، نه تزیین.
- **«ابطال» هیچ‌وقت در نوارِ بالای فاکتور نمی‌نشیند.** برگشت‌ناپذیر است؛ جایش ته کشوی
  «⋯ بیشتر» است با رنگِ خطر و تأییدِ جدا. نوارِ بالا فقط سه دکمه دارد: ثبتِ پرداخت،
  بیشتر، بستن. **دکمه‌ی چهارم به آن اضافه نکن.**
- **حذفِ کاملِ فاکتور فقط روی فاکتورِ باطل‌شده باز است — دو در، نه یکی.** برای پاک‌کردن
  باید اول عمداً «ابطال» بزنی (تأییدِ خودش) و بعد «🗑 حذفِ کاملِ فاکتور» (تأییدِ دوم).
  دکمه هم‌جای «ابطال» ته کشوی «⋯ بیشتر» است و روی فاکتورِ زنده اصلاً نشان داده نمی‌شود.
  محافظ در **دو** لایه است: پنل، و خودِ `delete_invoice` در دیتابیس که فاکتورِ
  غیرِ‌باطل را با خطا رد می‌کند. لایه‌ی دیتابیس اصل است — پنل فقط تجربه را بهتر می‌کند
  (ARCH-02).
  **سه دامِ کلیدِ خارجی که با پرس‌وجو از خودِ دیتابیس پیدا شدند، نه از حافظه:**
  `payments` با `ON DELETE SET NULL` بسته است، یعنی با حذفِ فاکتور **پاک نمی‌شود** و
  یتیم می‌مانَد و **همچنان از ماندهٔ حسابِ مشتری کم می‌کند** · `telegram_orders` با
  `NO ACTION` بسته است و حذف را **با خطا می‌خواباند** · `returns` ستونِ `invoice_id`
  دارد ولی **هیچ کلیدِ خارجی‌ای ندارد**، پس دیتابیس جلویش را نمی‌گیرد و بی‌صدا آویزان
  می‌مانَد. تابع هر سه را صریح تمیز می‌کند؛ `invoice_items` خودش با CASCADE می‌رود.
  با یک تراکنشِ برگشت‌خورده روی دیتابیسِ واقعی سنجیده شد: مانده قبل از حذف
  ۲٬۰۰۰٬۰۰۰ (پرداختِ همان فاکتور) و بعدش دقیقاً ۵٬۰۰۰٬۰۰۰ — یعنی هیچ ردیفِ یتیمی
  نماند. **سه بررسیِ خودکار دارد** و دسترسی‌اش هم با `curl` وارسی شد: کلیدِ عمومی
  روی `rpc/delete_invoice` جوابِ `401 permission denied` می‌گیرد.
  ⚠️ **گرانتِ توابع در `db_invariants` ثبت نمی‌شود** — نه این یکی، نه `cancel_invoice`
  و بقیه. یعنی اگر روزی کسی از داشبورد `execute` را به `anon` بدهد، دروازه ساکت
  می‌مانَد. اگر خواستی این حفره را ببندی، یک بخشِ `function_grants` به
  `db_invariants.sql` و snapshot اضافه کن.
- **شماره‌ی فاکتور از `nextval('invoice_seq')` می‌آید، نه از `max(invoice_number)+1`.**
  تا قبل از افزودنِ حذفِ فاکتور این فرق عملاً دیده نمی‌شد — نمی‌شد بالاترین فاکتور
  را برداشت. حالا دیده می‌شود: **با `max+1`، حذفِ آخرین فاکتور همان شماره را دوباره
  می‌دهد** و دو سندِ متفاوت با یک شماره دستِ دو مشتری می‌رسد. اندازه‌گیری شد
  (تراکنشِ برگشت‌خورده): ۷۰۲۶ → ۷۰۲۷ → ۷۰۲۸، بعد حذفِ ۷۰۲۸ و بعدی **۷۰۲۹** نه ۷۰۲۸.
  هر دو مسیرِ ساختِ فاکتور یکی‌اند؛ `convert_telegram_order` خودش `create_invoice`
  را صدا می‌زند. **این را به «max+1» برنگردان.**
  - **پرشِ شماره عیب نیست.** ترتیب‌شمار با تراکنشِ برگشت‌خورده عقب نمی‌رود، پس هر
    ثبتِ نیمه‌کاره یک شماره خرج می‌کند. عمدی است: بینِ «پشتِ سرِ هم بودن» و «هیچ‌وقت
    تکراری نشدن»، دومی برنده است.
  - **ولی ترتیب‌شمار هیچ‌وقت نباید از داده عقب بیفتد** (بازگردانی از پشتیبان، یا یک
    `setval`ِ اشتباه). آن‌وقت فاکتورِ بعدی به قیدِ یکتا می‌خورد و **اصلاً ثبت نمی‌شود**.
    درست‌کردنش:
    `select setval('invoice_seq', (select max(invoice_number) from invoices), true);`
  - `invoices_invoice_number_key` (قیدِ یکتا) تنها چیزی است که شماره‌ی تکراری را از
    «خرابیِ خاموش» به «خطای بلند» تبدیل می‌کند. **قیدهای یکتا قبلاً اصلاً ردیابی
    نمی‌شدند** چون `db_invariants` فقط `contype='c'` را می‌گرفت؛ حالا بخشِ `uniques`
    هست. سه بررسیِ خودکار دارد و هر سه با جهشِ عمدی آزموده شدند.
- **⚠️ `#custView` در سند بعد از `#invoiceView` می‌آید، پس اگر هر دو باز باشند مشتری
  رویی است.** هر دو `.overlay` با z-index ۶۰ اند و ترتیبِ سند تصمیم می‌گیرد، نه
  ترتیبِ باز کردن. یک بار بعد از «ثبت پرداخت» فاکتور را باز کردم و **پشتِ صفحه‌ی
  مشتری گم شد** — پنجره واقعاً باز بود ولی دیده نمی‌شد. **راهِ حلش بالا بردنِ
  z-index نیست** (مقیاسِ نام‌دار را می‌شکند)؛ اول آن یکی را ببند.
  بررسیِ خودکارش «رویی» را از ترتیبِ واقعیِ `.overlay`های باز می‌خوانَد، نه از
  اینکه کدام `hidden` ندارد — چون هر دو باز بودند و همین گمراه‌کننده بود.
- **بعد از ثبتِ پرداخت، اگر از فاکتور آمده‌ای به همان فاکتور برگرد.** قبلاً روی
  صفحه‌ی مشتری با همان کادرِ خالیِ پرداخت می‌ماند و مالک گزارش کرد «انگار دوباره
  می‌پرسد، ولی پرداخت ثبت شده بود». حالا نشانِ تازه‌ی فاکتور را می‌بیند.
- **کشو (`.sheetwrap`) z-index ۶۰ دارد، نه بیشتر.** چون بعد از `#invoiceView` در سند
  می‌آید، خودبه‌خود رویش می‌نشیند. بالاتر بردنش مقیاسِ نام‌دار را می‌شکند.
- **`confirm()` و `alert()` ممنوع‌اند — از `await ask(msg)` استفاده کن.** همان قرارداد را
  دارد (`true` یعنی ادامه بده) ولی `<dialog>` است: در لایه‌ی رویی می‌نشیند، Esc می‌بندَدش،
  Tab پشتش نمی‌رود، و متنِ چندخطیِ فارسی را درست نشان می‌دهد. اگر خطِ اولِ پیام با ⚠️
  شروع شود و بعدش خطِ خالی باشد، آن خط جدا و برجسته می‌آید و دکمه‌ی تأیید قرمز می‌شود —
  یعنی هشدارهای خطرناک را طوری بنویس. **هر تابعی که `ask` صدا می‌زند باید `async` باشد.**
- **خروجی همیشه با `esc()` فرار داده می‌شود.** هیچ داده‌ی کاربر بدونِ آن داخل
  `innerHTML` نرود.
- **دستورِ درون‌خطی:** الگوی `onclick="rowTap('prod','${p.id}',event)"` — گیومه‌ی تکی.
  از `JSON.stringify` داخلِ attribute استفاده نکن؛ گیومه‌ی دوتایی می‌سازد و دستور را
  قطع می‌کند (باگِ واقعی بود).
- **لایه‌بندیِ z-index (نام‌دار، از این خارج نشو):**
  نوارِ بالا ۳۰ · نوارِ پایین ۴۰ · نوارِ انتخابِ گروهی و نوارِ به‌روزرسانی ۵۰ ·
  پنجره‌ها ۶۰ · پیامِ شناور ۸۰.
  **چیزی که دکمه دارد حق ندارد بالاتر از ۶۰ بنشیند** — لمسِ دکمه‌های پنجره را می‌دزدد.
  پیامِ شناور استثناست چون `pointer-events:none` دارد. `#updBar` یک بار `z-index:9999`
  داشت و از بررسی در نرفته بود چون **با کد ساخته می‌شود** و فقط وقتی نسخه‌ی تازه در
  انتظار باشد وجود دارد؛ بازرس روی وب‌کیت گرفتش. **عنصری که با کد ساخته می‌شود را
  هم در مقیاس حساب کن.**
  هم‌ترازیِ `#updBar` با `.bulkbar` یک دام دارد: هر دو `bottom` یکسان دارند و چون
  نوارِ به‌روزرسانی بعداً به `body` اضافه می‌شود، می‌افتد رویش. بالابردنش جواب نمی‌دهد
  چون نوارِ گروهی `flex-wrap` دارد و ارتفاعش ثابت نیست؛ به‌جایش
  `body:has(#bulkBar:not(.hidden)) #updBar{display:none}` گذاشته شده — خبرِ نسخه‌ی
  جدید عجله ندارد و همین که انتخابِ گروهی بسته شد خودش برمی‌گردد.
- **تم:** رنگ‌ها با متغیرِ CSS. رنگِ ثابت ننویس. فاکتور و کارتِ گزارش با کلاسِ
  `force-light` عکس گرفته می‌شوند — یعنی روی صفحه با تم عوض می‌شوند ولی **عکسی که
  برای مشتری می‌رود همیشه روشن است**. این را نشکن.
- **شبکه:** `sbFetch` مهلت (۲۵ ثانیه) و لغو دارد و پیامِ فارسی می‌دهد.
- **دسترس‌پذیری:** تابعِ `a11yFix` خودکار لیبل‌ها را به ورودی‌ها وصل می‌کند و به
  ردیف‌های کلیک‌پذیر نقشِ دکمه و دسترسیِ صفحه‌کلید می‌دهد. دستی تکرارش نکن.
- **رسانه:** ویدیوی کالا در تلگرام ذخیره می‌شود نه در storage. **سقفِ ۵۰ مگ قانونِ
  خودِ تلگرام است، نه انتخابِ ما** — ربات بیشتر از آن آپلود نمی‌کند. زیرِ ۵۰ مگ هیچ
  فشرده‌سازی‌ای انجام نمی‌شود و فایل دست‌نخورده بالا می‌رود. بالای ۵۰ مگ از ۱۴۴۰ شروع
  می‌کند و فقط وقتی پایین‌تر می‌رود که جا نشود یا خروجی سالم درنیامده باشد.
  **۱۹۲۰ را امتحان نکن:** آزمایش شد، رمزگذار بی‌سر‌و‌صدا فایلِ خرابِ چندکیلوبایتی می‌دهد.
  به همین دلیل `medVideoUsable` هر خروجی را قبل از آپلود باز می‌کند و بُعدش را می‌سنجد.
  (سقفِ ۵۰ مگابایت،
  فشرده‌سازی با صدا در خودِ مرورگر). بعد از آپلود، تابعِ `medPrime` آن را به تلگرام
  می‌فرستد و نسخه‌ی storage را پاک می‌کند.
- **نشانیِ فیلم ژتون می‌خواهد — `<video src>` را ساده نکن.** از نسخه‌ی ۳۹ ربات،
  `?media=` بی‌ژتون `401` می‌دهد. پنل اول `medTok(kind)` را صدا می‌زند که با هدرِ
  Authorization از `?mediatoken=` یک ژتونِ یک‌ساعته می‌گیرد، بعد آن را در `&t=`
  می‌گذارد. **اگر `renderMedia` را «تمیز» کنی و نشانی را مستقیم بسازی، فیلم
  می‌میرد** — و چون `onerror` پیامِ «حجمش زیاده» را نشان می‌دهد، شبیهِ همان
  محدودیتِ ۲۰ مگ به نظر می‌رسد و ساعت‌ها دنبالِ باگِ اشتباه می‌گردی.
  حالتِ «ژتون هنوز نیامده» عمداً یک پیامِ انتظار دارد، نه `<video>`ِ بی‌نشانی.
  روالِ کاملش در `bot/README.md`. هشت بررسیِ خودکار دارد (شبیه‌سازِ ربات).
- **فیلمِ بالای ۲۰ مگ در پنل پخش نمی‌شود و این عیب نیست — تصمیمِ مالک است که
  همین‌طور بماند (۱۴۰۵/۰۵/۲۳).** تلگرام نامتقارن است: ربات تا **۵۰ مگ آپلود**
  می‌کند ولی `getFile` فقط تا **۲۰ مبی‌بایت دانلود** می‌دهد. پس هر فیلمِ بینِ ۲۰ و
  ۵۰ مگ به مشتری می‌رسد ولی `?media=` رویش `413 toobig` برمی‌گردانَد و پنل پیامِ
  `medVidFail` را نشان می‌دهد. **آن پیام درست است، دنبالِ باگ نگرد.**

  اندازه‌گیری روی هر ۱۴ کالای دارای فیلم (۱۴۰۵/۰۵/۲۳):

  | نتیجه | حجم |
  |-------|-----|
  | `200` | ۱۲٫۳ · ۱۸٫۶ · ۱۹٫۸ · ۲۰٫۹ مگابایت |
  | `413` | هفت کالا |
  | `404` | دو کالا (فیلمِ جین ندارند) |

  بزرگ‌ترین موفق ۲۰٬۸۵۲٬۵۶۵ بایت است — درست زیرِ ۲۰ مبی‌بایت. یعنی مسیر سالم است
  و مرز دقیقاً همان سقفِ تلگرام است، نه چیزِ دیگر.

  **راهِ حلی که مطرح شد و مالک ردش کرد:** پایین آوردنِ آستانه‌ی فشرده‌سازی از ۵۰ به
  ۲۰ مگ، تا هر فیلمی در پنل هم باز شود. هزینه‌اش افتِ کیفیتِ فیلمی است که دستِ
  مشتری می‌رسد، و مالک گفت نمی‌ارزد. **بدونِ پرسیدن این آستانه را عوض نکن.**
  (این آزمایش ~۶۸ مگابایت پهنای باند خرج کرد؛ اگر لازم شد دوباره بسنجی، بدان که
  رایگان نیست.)
- **چشمِ ناظر (`reportError`) — خطاهای گوشیِ کاربر در جدولِ `client_errors`.**
  مالک تنها کاربر است و روی گوشی کار می‌کند؛ بدونِ این، خرابیِ خاموش فقط وقتی معلوم
  می‌شود که خودش ببیند و بتواند توصیفش کند. **دیدنشان:**
  ```sql
  select created_at, kind, screen, app_version, message
  from client_errors order by created_at desc limit 50;
  ```
  جدول عمداً **هیچ سیاستِ SELECT ندارد** — از راهِ API خوانده نمی‌شود، فقط با نقشِ
  سرویس. سیاستش فقط `insert` برای کاربرِ واردشده است.

  **شش قاعده‌ی سختِ این تکه؛ هیچ‌کدام را نشکن** (چیزی که خودش باگ بسازد بدتر از
  نداشتنش است) — هر چهار بررسیِ خودکارش هم همین‌ها را می‌سنجند:
  ۱. هرگز `throw` نکند ۲. حلقه نسازد (`_errIn`) ۳. سقفِ ۸ گزارش در نشست و
  بدونِ تکرارِ پیامِ یکسان ۴. منتظرِ پاسخ نمانَد ۵. **هیچ داده‌ی مشتری نفرستد** —
  `_errScrub` پارامترِ آدرس‌ها را می‌بُرد و رشته‌های ۷ رقم به بالا را پاک می‌کند،
  چون متنِ خطای PostgREST خودِ پرس‌وجو را در خود دارد ۶. با `fetch`ِ خام برود نه
  `sbFetch`، وگرنه تازه‌کردنِ توکن و پرت‌شدن به صفحه‌ی ورود را راه می‌اندازد؛ و
  قبل از ورود ساکت بماند.
- **پنل عمداً زوم نمی‌شود — چهار تکه، و هر چهار لازم‌اند** (`waiver: panel-no-zoom`).
  خواستِ صریحِ مالک بود: «مثلِ یه برنامه‌ی واقعی شه». هیچ‌کدام به‌تنهایی کافی نیست:
  | تکه | چه چیزی را می‌بندد |
  |-----|--------------------|
  | `maximum-scale=1, user-scalable=no` در متا | زومِ اندروید، و آیفون **فقط در حالتِ نصب‌شده** (`display:standalone`) |
  | `touch-action:manipulation` روی `html,body` | زومِ دو-ضربه، روی هر دو |
  | `preventDefault` روی `gesturestart/change/end` | پینچِ سافاری در تبِ عادی — iOS از نسخه‌ی ۱۰ متا را آنجا نادیده می‌گیرد |
  | **`input,select,textarea{font-size:16px}`** | زومِ خودکارِ آیفون موقعِ فوکوس |
  **مهم‌ترینش آخری است و ربطی به متا ندارد:** ۱۶ پیکسل آستانه‌ی خودِ iOS است؛ روی
  کادرِ کوچک‌تر سافاری موقعِ تپ‌کردن صفحه را زوم می‌کند و **دیگر برنمی‌گردد**. قبلاً
  ۱۵ بود. **زیرِ ۱۶ نبرش** — دکمه روی ۱۵ ماند چون فوکوسِ دکمه زوم نمی‌آورد.
  بزرگ‌کردنِ قلم در ۳۲۰ پیکسل روی هر ۳۲ صفحه سنجیده شد: هیچ سرریزِ افقی‌ای نساخت.
  ⚠️ **هزینه‌اش:** زومِ دستی روی عکسِ کالا هم دیگر کار نمی‌کند. مالک این را پذیرفت.
  بررسیِ خودکارش هر چهار تکه را جدا می‌سنجد.
- **`viewport-fit=cover` به متای viewport اضافه نکن.** الان نیست، و همین درست است:
  بدونِ آن، iOS کلِ صفحه را داخلِ ناحیه‌ی امن نگه می‌دارد و `.bottomnav{bottom:0}`
  **بالای** نوارِ خانه‌ی آیفون می‌نشیند. با اضافه‌کردنش (که «بهبود»ِ رایجی به‌نظر
  می‌رسد) نوارِ پایین می‌رود زیرِ نوارِ خانه و دکمه‌هایش سخت‌لمس می‌شوند — و چون
  `env(safe-area-inset-bottom)` فقط در یک جا (`.sheet-card`) هست، جبرانش نمی‌شود.
  اگر روزی واقعاً لازم شد، **اول** به همه‌ی عنصرهای چسبیده به پایین padding بده.
- **`nav()` همگام نیست.** داخلِ `startViewTransition` می‌پیچد و جابه‌جایی یک فریم بعد
  انجام می‌شود، پس **بلافاصله بعد از `nav()` وضعیتِ DOM را نخوان** — کهنه است. برای
  «الان کدام صفحه‌ایم؟» از `CUR_SCR` استفاده کن که `nav()` همگام تنظیمش می‌کند.
  یک بار `openProductForm` شرطش را از روی DOM می‌خواند و فرمِ کالا گاهی نامرئی می‌مانْد؛
  با `--repeat` بازرس پیدا شد، چون با یک اجرا هر بار یک جواب می‌داد.
- **PWA:** `sw.js` نسخه‌دار است، فایل‌ها را تک‌تک کش می‌کند (نه یکجا)، صفحه‌ی آفلاین
  دارد و API را کش نمی‌کند. `sw.js` خودش کش نمی‌شود تا به‌روزرسانی گیر نکند.
- **`controllerchange` فقط وقتی رفرش می‌کند که نسخه عوض شده باشد.** بارِ اولی که
  سرویس‌ورکر نصب می‌شود و کنترل را می‌گیرد (`clients.claim`) هم همین رویداد را می‌زند،
  ولی نسخه‌ای عوض نشده. بدونِ شرطِ `hadController`، همان چند ثانیه‌ی اولِ اولین باز شدن
  صفحه یک‌بار می‌پرید و هرچه کاربر تایپ کرده بود (مثلاً ایمیل و رمز) می‌رفت.
  **شرط را برندار.**
- **تازه‌کردنِ توکن «تک‌پروازه» است (`_refreshing`).** صفحه‌ی بینش‌ها هفت درخواست را با
  هم می‌فرستد؛ اگر توکن همان لحظه تمام شده باشد هر هفت‌تا با یک `refresh_token` یکسان
  می‌روند سراغِ سرور. سوپابیس توکنِ تازه‌سازی را **می‌چرخانَد** — اولی مصرفش می‌کند و
  بقیه با توکنِ سوخته می‌رسند؛ سوپابیس این را «استفاده‌ی دوباره» می‌بیند و می‌تواند کلِ
  خانواده‌ی توکن را باطل کند، یعنی کاربر وسطِ کار پرت می‌شود بیرون. اندازه گرفته شد:
  قبلش ۷ تا (و در طوفانِ ۴۰۱، ۱۴ تا)، حالا دقیقاً ۱. `AUTH_GEN` هم مکمّلش است: اگر
  درخواستی ۴۰۱ گرفت ولی در همان فاصله یکی دیگر توکن را تازه کرده بود، فقط دوباره
  می‌رود — تازه نمی‌کند. **دو بررسیِ خودکار در بازرس دارد.**
- **هر رنگی که `var(--x)` می‌نویسی باید در **چهار** جا تعریف شده باشد:** `:root`،
  `html[data-theme="dark"]`، `.force-light` و بلوکِ `@media print`. متغیرِ تعریف‌نشده
  خطا نمی‌دهد — مرورگر بی‌صدا رهایش می‌کند و رنگ به ارث می‌رسد. یک‌بار `--danger` و
  `--accent` در ۲۶ جا استفاده شده بودند و **هیچ‌جا تعریف نشده بودند**: یعنی دکمه‌ی
  «ابطالِ فاکتور»، کارتِ بدهکاران و همه‌ی هشدارها اصلاً قرمز نبودند. یک بررسیِ خودکار
  دارد که هر متغیرِ لازم را روی `documentElement` می‌سنجد.
- **تخفیفِ پیش‌فاکتور همیشه «تومان» ذخیره می‌شود** (`saveQuote` خودش درصد را تبدیل
  می‌کند). پس `_fillFormFromQuote` **باید** `nDiscMode` را برگرداند روی `t`. بدونِ آن،
  اگر کاربر قبلش روی «درصد» بوده باشد، ۵۰٬۰۰۰ تومان به‌عنوان ۵۰٬۰۰۰٪ خوانده می‌شود،
  به ۱۰۰٪ چفت می‌شود و **کلِ فاکتور مجانی می‌شود** (اندازه‌گیری شد: مبلغِ نهایی ۰).
  بررسیِ خودکار دارد.
- **`ask()` صف دارد (`_askChain`).** یک `<dialog>` بیشتر نداریم، پس دو پرسشِ هم‌زمان
  همدیگر را خراب می‌کنند: دومی متنِ اولی را عوض می‌کند، `showModal` روی پنجره‌ی باز
  خطا می‌دهد و می‌افتد روی `confirm`ِ بومی، و Promiseی اولی تا ابد معلق می‌مانَد.
  با دو بار زدنِ سریعِ یک دکمه روی گوشی پیش می‌آید. **این صف را برندار.**
- **هیچ صفحه‌ای حق ندارد با دادهٔ ناقص «همه‌چی مرتبه» بگوید.** `insChurn`،
  `insAffinity` و `insAlerts` این محافظ را نداشتند و اگر یکی از هفت درخواستِ
  `openInsights` شکست می‌خورد، «مشتریِ در حالِ ریزشی نداری 👌» یا «هیچ هشداری نداری 🎉»
  نشان می‌دادند — خبرِ خوشِ دروغ. همین‌طور `renderQuotes` و `renderDiscounts` خطای
  شبکه را می‌بلعیدند و «چیزی ثبت نشده» می‌گفتند. **قاعده: `catch` خالی روی خواندنِ
  داده ممنوع — یا خطا نشان بده یا `insBlocked`.**
- **عکس‌گرفتن از عنصرِ بلند با `renderToCanvas`، نه مستقیم `html2canvas`.** سافاریِ
  آیفون سقفِ ارتفاعِ بوم دارد؛ کاتالوگِ ۳۰۰ کالا در مقیاسِ ۲ از آن رد می‌شود و — بدتر
  از خطا — گاهی **بومِ سفید** می‌دهد که بی‌سروصدا برای مشتری فرستاده می‌شود.
  `renderToCanvas` مقیاس را از روی `scrollHeight` حساب می‌کند و بومِ خالی را رد می‌کند.
- **فرستادنِ عکس با `shareOrDownload`، نه `navigator.share` مستقیم.** بینِ کلیکِ کاربر
  تا آماده شدنِ عکس (بارگذاریِ h2c + رندر + `toBlob`) «اجازه‌ی کاربر» منقضی می‌شود و
  روی سافاری `share` با `NotAllowedError` رد می‌شود. قبلاً `catch(e){}` بود، یعنی
  کاربر دکمه را می‌زد و **هیچ اتفاقی نمی‌افتاد**. حالا فقط `AbortError` (لغوِ خودِ
  کاربر) ساکت است و بقیه می‌افتند روی دانلود.
- **پاسخِ کهنه نباید روی پاسخِ تازه بنشیند (`seqStart`/`seqStale`).** سه جفت تابع سرِ
  یک جعبه با هم مسابقه می‌دهند: `renderInvoices`↔`searchInvoices` روی `invList`،
  `gsRecent`↔`gsRun` روی `searchBody`، و `renderCustomers`↔`renderCustomersByProduct`
  روی `custList`. هرکدام درخواستِ خودش را می‌فرستد و **هرکه دیرتر برسد آخر می‌نویسد —
  نه هرکه کاربر آخر خواسته.** روی اینترنتِ کند این یعنی کاربر کادر را پاک می‌کند ولی
  هنوز نتیجه‌ی جست‌وجو را می‌بیند. الگو: قبل از `await` یک `const _q=seqStart('نامِ جعبه')`
  و بعد از `await` یک `if(seqStale('نامِ جعبه',_q))return;` — **شاخه‌ی `catch` را هم
  یادت نرود**، وگرنه خطای درخواستِ کهنه روی نتیجه‌ی درست می‌نشیند. هر تابعِ تازه‌ای که
  در یکی از این سه جعبه بنویسد باید همین را داشته باشد. دو بررسیِ خودکار دارد.
- **دکمه‌ی برگشتِ گوشی پنجره‌ها را می‌بندد.** هر پنجره‌ای که باز شود یک ورودیِ تاریخچه
  می‌گذارد و «برگشت» بالایی را می‌بندد؛ وقتی پنجره‌ای باز نیست، از هر تبی می‌رود
  داشبورد و از داشبورد می‌گذارد از برنامه بیرون برود. **این از روی کلاسِ `hidden` با
  `MutationObserver` کار می‌کند، نه از روی صدا زدنِ توابعِ باز/بستن** — پس هر جای تازه‌ای
  که پنجره باز کنی خودکار پوشش دارد و لازم نیست چیزی صدا بزنی. اگر پنجره‌ای پاک‌سازیِ
  اضافه لازم دارد، فقط اسمش را به `OV_ONCLOSE` اضافه کن. `_ovMarks` هر بار که پنجره‌ای
  باز نباشد صفر می‌شود تا کاربر هیچ‌وقت در تاریخچه گیر نیفتد. **سه بررسیِ خودکار دارد.**
  **در `popstate`، وقتی هیچ پنجره‌ای باز نیست هیچ کاری نکن** — ولی دلیلش آن چیزی نیست
  که اول نوشتم، و این تصحیح مهم است:

  یک بار «برگشت از هر تبی یعنی داشبورد» آنجا گذاشتم، بازرسِ وب‌کیت قرمز داد
  («فرمِ کالا از هر صفحه‌ای دیده می‌شود») و من حدس زدم `history.go(-n)` روی بعضی
  مرورگرها چند `popstate` می‌دهد. **هر دو حدسم غلط بود و اندازه‌گیری ردشان کرد:**

  ۱. یک صفحه‌ی خالی ساختم که سه ورودی می‌گذارد و `history.go(-2)` می‌زند:
     کرومیوم **۱** popstate، وب‌کیت هم **۱**. هیچ مرورگری چندتا نمی‌دهد.
  ۲. کدِ قدیمی را با ثبتِ هر `nav` و هر `popstate` مجهز کردم و بازرسِ واقعی را رویش
     زدم: **هیچ popstateای به آن شاخه نرسید** (همه با `lock=true` آمدند و محافظ
     مصرفشان کرد). `nav('dashboard')`ها همه از خودِ گردشِ صفحه‌های بازرس بودند.

  علتِ واقعیِ قرمزیِ آن بررسی: بازرس گردشِ صفحه‌ها را چند بار تکرار می‌کند (تمِ تیره،
  روشن، صفحه‌ی باریک) و آخرین دور روی `nav('dashboard')` تمام می‌شود؛ آن‌وقت
  `prodFormWrap` هنوز باز است ولی داخلِ `#scr-products`ِ مخفی. **خودِ بررسی معیوب بود،
  نه کد** — به وضعیتِ جامانده از مرحله‌ی قبل تکیه می‌کرد و ترتیبِ دورها روی هر موتور
  فرق داشت، پس نتیجه‌اش شانسی بود. حالا بررسی خودش فرم را باز می‌کند.

  آن شاخه برداشته ماند، چون امکانِ لوکسی بود و شواهدی هم به سودش نیست — ولی
  **برداشتنش «رفعِ باگ» نبود.** اگر روزی خواستی برش گردانی، ایرادی ندارد؛ فقط این بار
  با آزمایش تصمیم بگیر نه با حدس.

  **درسِ اصلی: «سبز شد» با «ثابت کردم» یکی نیست.** یک تغییر و یک سبز شدن، رابطه‌ی علّی
  نمی‌سازد. اگر مکانیزم را نمی‌توانی نشان بدهی، بنویس که نمی‌توانی.

## ۶. دیتابیس

شِمای کامل در `auditor/schema.txt` است. قوانینِ یکپارچگی روی خودِ دیتابیس گذاشته شده‌اند
(CHECK constraint و trigger): مبلغ و تعدادِ نامنفی، تخفیفِ کمتر از جمع، هم‌خوانیِ جمع‌ها،
و ممنوعیتِ پرداخت روی فاکتورِ باطل. **این‌ها را دور نزن؛ اگر تغییری لازم شد، اول با
کاربر هماهنگ کن.**

فرمولِ ماندهٔ حسابِ مشتری:
```
مانده = مجموع(کلِ فاکتورهای غیرباطل − پرداختِ سرِ فروش) − مجموع(پرداخت‌های بعدی)
```

**دو راهِ جدا برای «پول گرفتم» وجود دارد و فرقشان مهم است** (باگی که مالک گزارش کرد،
۱۴۰۵/۰۵/۳۱):
- **«💰 ثبت پرداخت»** یک ردیف در `payments` می‌سازد. `paid_amount` فاکتور را دست نمی‌زند.
- **«✅ تسویه شد»** (`settle_invoice`) خودِ `paid_amount` فاکتور را بالا می‌برد.

چون فرمولِ بالا این دو را **جداگانه** کم می‌کند، هر تغییری اینجا باید بپرسد «آیا این
مبلغ دارد دوبار کم می‌شود؟». دو چیز که همین‌جا شکسته بود و درست شد:
۱. `invoices.status` فقط سرِ ساختِ فاکتور حساب می‌شد. با «ثبت پرداخت»، ماندهٔ حسابِ
   مشتری درست صفر می‌شد ولی نشانِ فاکتور تا ابد **«نسیه»** می‌مانْد. حالا تریگرِ
   `trg_payments_sync_invoice_status` نشان را از روی `paid_amount + پرداخت‌های همان
   فاکتور` تازه می‌کند — و **عمداً به `paid_amount` دست نمی‌زند**، وگرنه مانده دوبار
   کم می‌شد.
۲. `settle_invoice` مقدارِ `paid_amount` را برابرِ کلِ مبلغ می‌گذاشت بی‌آنکه
   پرداخت‌های ثبت‌شده را ببیند. اگر مالک اول «ثبت پرداخت» و بعد «تسویه شد» را می‌زد،
   همان مبلغ دوبار کم می‌شد و مانده **منفی** می‌شد — یعنی پنل می‌گفت *تو* به مشتری
   بدهکاری. اندازه‌گیری شد: `−۶٬۵۴۰٬۰۰۰`. حالا پرداخت‌های همان فاکتور را کسر می‌کند.

مهاجرتش: `db/migrations/2026-08-22_invoice_status_follows_payments.sql` (راهِ برگشت
تهِ خودِ فایل نوشته شده). **روشِ تستِ بی‌خطر که جواب داد:** یک بلوکِ `DO` که مشتری و
فاکتور و پرداختِ آزمایشی می‌سازد، وضعیت را می‌خوانَد و آخرش عمداً `raise exception`
می‌زند تا کلِ تراکنش برگردد — نتیجه در متنِ خطا برمی‌گردد. شماره‌ی فاکتور هم نمی‌سوزد
چون `invoice_number` دستی داده می‌شود نه از `nextval`. **برای هر آزمایشِ پولیِ بعدی
روی دیتابیسِ واقعی از همین الگو استفاده کن.**

## ۷. وضعیتِ فعلی

- بازرس سبز است روی **هر دو موتور** (اندازه‌گیری‌شده در ۱۴۰۵/۰۵/۳۱، کدِ خروجی ۰):
  کرومیوم **۱۲۱ پاس**، وب‌کیت **۱۱۸ پاس + ۳ هشدار** (کدکِ ویدیو). با `-r 3` روی کرومیوم
  یکی بیشتر می‌شود (آن یکی خودِ «هر بررسی نتیجه‌ی یکسان داد» است).
  **۹۲ بررسیِ ویژه‌ی پروژه** روی ۳۲ صفحه.
- **عکسِ همه‌ی صفحه‌ها گرفته می‌شود، نه سی‌تای اول.** `screens[:30]` یک سقفِ ثابت بود که
  **همیشه آخرین صفحه‌ها را می‌انداخت بیرون** — یعنی دقیقاً همان صفحه‌ای که تازه ساخته‌ای.
  بی‌صدا هم بود: «۳۰ عکس ذخیره شد» می‌گفت در حالی که ۳۲ صفحه داشتیم.
- **دروازه دیگر یک بازرس نیست، شش‌تاست — و دیگر به یادِ کسی بند نیست.**
  `.github/workflows/gates.yml` روی هر PR اجرایشان می‌کند:
  | دروازه | چه می‌سنجد | هزینه |
  |--------|------------|-------|
  | `guidelines/self-check.py` | سازگاریِ درونیِ دستورالعمل (۳۳ بررسی) | ثانیه |
  | `auditor/server_audit.py` | متنِ `bot/index.ts` + ثابت‌های دیتابیس (۴۱ بررسی) | ثانیه |
  | `auditor/botsim/run.ts` | **رفتارِ** خودِ ربات با دیتابیس و تلگرامِ استاب‌شده (۳۲ بررسی) | ثانیه |
  | `auditor/audit.py` × ۳ | پنل (۱۲۱)، ابزارِ خرج (۷۱)، واردکردنِ فایل (۴۲) با کرومیوم | ~۴ دقیقه |
  | `auditor/tests/run.py` | **خودِ بازرس** — ۹ مورد × دو نمونه (معیوب و سالم) | ~۹۰ ثانیه |
  کارِ مرورگری فقط وقتی اجرا می‌شود که `panel/` یا `expenses/` یا `importer/` یا
  `auditor/` عوض شده باشد — ولی اگر **نشود فهمید** چه عوض شده، اجرا می‌شود نه اینکه
  رد شود. اولین نسخه برعکس بود و بی‌صدا در هشت ثانیه سبز می‌داد بی‌آنکه چیزی سنجیده باشد.
- **بازرس هم خودش بازرس دارد (`auditor/tests/`).** هر مورد دو نمونه دارد: یکی
  عمداً معیوب که **باید** گرفته شود، یکی سالم که **نباید** قرمز بدهد. نصفِ ارزشِ
  کار در نمونه‌ی سالم است — بدونِ آن معلوم نمی‌شود بررسی همان باگ را می‌گیرد یا به
  همه‌چیز قرمز می‌دهد. تا امروز **پنج هشدارِ نادرستِ واقعی** در `audit.py` را همین
  لو داده: تعریفِ تابع بعد از `;`، شناسه‌ی ساخته‌شده در زمانِ اجرا
  (`getElementById('tab'+k)`)، امضای کورِ «بعد از کلیک چیزی عوض شد؟»، کامنتِ HTML
  که پشته‌ی تگ‌ها را به‌هم می‌ریخت، و یک نمونه‌ی خودِ من که غلط بود نه بازرس.
- **ثابت‌های دیتابیس دیگر دستی نیستند.** `auditor/db_invariants.snapshot.json` در
  مخزن است و `server_audit.py` بدونِ هیچ سوییچی می‌سنجدش، پس آن بررسی‌ها (RLS روی
  هر ۱۸ جدول، بسته بودنِ `app_config` و `bot_admins`، فقط‌خواندنی بودنِ فاکتور،
  ۱۳ قیدِ CHECK، ۲۰ قیدِ یکتا، ۲ تریگر، دسترسیِ نماها و سلامتِ شماره‌ی فاکتور)
  روی هر PR اجرا می‌شوند. قبلاً بدونِ `--db` بی‌صدا رد
  می‌شدند، یعنی عملاً هیچ‌وقت.
  **ولی عکس با دیتابیسِ زنده یکی نیست** — اگر کسی فردا یک قید را از داشبورد
  بردارد، تا وقتی عکس تازه نشود اینجا سبز می‌مانَد. برای همین تاریخِ عکس همیشه
  چاپ می‌شود و اجرای هفتگیِ `gates.yml` با `--max-age-days 60` کهنه بودنش را
  قرمز می‌کند. **تازه‌کردن:** پرس‌وجوی `auditor/db_invariants.sql` را بزن و خروجی
  را با `taken_at` در snapshot بگذار. آخرین بار: ۱۴۰۵/۰۵/۳۱ — بدونِ هیچ انحرافی.
- **ربات تا ۱۴۰۵/۰۵/۲۳ هیچ تستِ رفتاری نداشت؛ حالا ۹ تا دارد** (`auditor/botsim/`).
  خودِ `index.ts` اجرا می‌شود، نه بازنویسی‌اش؛ فقط دیتابیس و `fetch` استاب‌اند و تماس
  با هر آدرسی جز `api.telegram.org` بلند خطا می‌دهد. **استاب هم خودش سخت‌گیر است**:
  فیلتر یا `rpc`ی که نشناسد خطا می‌دهد، چون استابی که بی‌صدا `undefined` بدهد حالتی
  می‌سازد که در دیتابیسِ واقعی ممکن نیست (TEST-05 — یک بار همین یک «باگِ پول»ِ
  خیالی ساخت). چهار باگِ عمدی کاشته شد و هر چهار قرمز دادند.
  **یکی‌شان اول در رفت و درسِ خوبی داد:** `Math.max(0, …)` با تخفیفِ دقیقاً ۱۰۰٪
  هم‌ارز است — حاصل صفرِ دقیق می‌شود، پس آن محافظ اصلاً سنجیده نمی‌شد. مرزِ ۱۵۰٪ که
  اضافه شد، `−۱٬۶۹۵٬۰۰۰` داد. **بررسیِ تازه که نوشتی، همین کار را با آن بکن.**
- آخرین نسخه‌ی کش: `mlq-2026-08-22-76`.
- **آمارِ بازدید و نرخِ تبدیل — از ۱۴۰۵/۰۵/۳۰ (نسخه‌ی ۴۱ ربات).**
  دو چیز را قبل از هر کاری بدان، وگرنه دنبالِ باگی می‌گردی که وجود ندارد:

  ۱. **سایتِ عمومی نداریم.** ورکر فقط `panel/` را سرو می‌کند و پنل پشتِ ورود است
     (ثبت‌نام بسته، یک کاربر). مشتری از **ربات** خرید می‌کند. پس «بازدید» یعنی
     بازدید از ربات، و منبعش `bot_events` است نه هیچ ابزارِ تحلیلِ وب.
  ۲. **این صفحه گذشته را نشان نمی‌دهد و این عیب نیست.** قبل از نسخه‌ی ۴۱ هیچ
     رویدادی ذخیره نمی‌شد: `telegram_sessions` جای هر نفر یک ردیف دارد که سرِ جا
     بازنویسی می‌شود و `created_at` هم ندارد. پس تاریخچه‌ای در کار نبود که
     بشود بازسازی‌اش کرد. صفحه با دادهٔ خالی **صفر نشان نمی‌دهد** — می‌گوید
     «هنوز چیزی جمع نشده»، چون «نرخِ تبدیل ۰٪» یعنی «هیچ‌کس نخرید» و آن ادعای
     غلطی است. بررسیِ خودکارش هست.

  **مالک نمی‌تواند آمارِ خودش را ببیند — و این یک بار شبیهِ باگ شد.** اولین تستِ
  مالک چهارده رویدادِ کاملِ قیف ساخت (`start` تا `pay_click`) و صفحه گفت «هنوز
  چیزی جمع نشده»، چون `chat_id`ِ او در `bot_admins` است. درست کار می‌کرد و
  گیج‌کننده بود. برای همین کلیدِ **«ترافیکِ خودم رو هم حساب کن»** اضافه شد که
  جدولِ خام را می‌خواند.
  **این کلید عمداً ذخیره نمی‌شود** و با هر بار باز شدنِ بینش‌ها خاموش می‌گردد —
  یک «روشن»ِ فراموش‌شده هفته‌ها بعد نرخِ تبدیل را بی‌صدا باد می‌کرد و هیچ‌کس
  دلیلش را نمی‌فهمید. چهار بررسیِ خودکار دارد و مهم‌ترینشان همین خاموش شدنِ
  خودکار است.
  ضمناً همان چهارده ردیف چیزی را ثابت کرد که با کلیدِ cron نتوانسته بودم بسنجم:
  نسخه‌ی ۴۰ اصلاً `logEvent` نداشت، پس وجودشان یعنی ایزوله‌ی زنده **۴۱** است.

  **پنل از `bot_events` نمی‌خواند، از `bot_events_public` می‌خواند.** چهار
  `chat_id` در `bot_admins` هست و هر چهار خودِ مالک است؛ گشت‌وگذارِ خودش اگر
  «بازدیدکننده» شمرده شود نرخِ تبدیل بی‌معنی می‌شود — به‌خصوص حالا که تعدادِ
  مشتری‌ها کم است و یک نفر کلِ عدد را جابه‌جا می‌کند. همین برای
  `bot_carts_open` (سبدهای رهاشده) هم صادق است. **مستقیم از جدول نخوان.**

  **هر مرحله‌ی قیف «دستِ‌کم تا اینجا رسید» است، نه «دقیقاً این کار را کرد».**
  چون مشتری می‌تواند از جست‌وجو مستقیم به کارتِ کالا برسد، شمردنِ دقیق قیف را
  می‌شکند و مرحله‌ی پایین از بالایی بزرگ‌تر درمی‌آید. بررسیِ «قیف یکنواخت است»
  همین را نگه می‌دارد.

  **فهرستِ یازده رویداد در چهار جا تعریف شده و باید یکی بمانَد:** `bot/index.ts`،
  استابِ شبیه‌ساز، قیدِ CHECK دیتابیس، و `EV_DEPTH` در پنل. `server_audit.py`
  هر چهار را مقابله می‌کند، پس دیگر لازم نیست یادت بماند.
  **چهارمی را اول فقط در مستند هشدار داده بودم و همان غلط بود** — در این مخزن
  هشدارِ متنی بعد از چند بار خوانده نمی‌شود. خرابی‌اش هم بی‌صداترین است: رویدادی
  که در `EV_DEPTH` نباشد عمقِ صفر می‌گیرد و **از کلِ قیف می‌افتد بیرون** — نه
  خطایی، نه صفری، فقط یک نرخِ تبدیلِ کمتر از واقعیت که کسی دلیلش را نمی‌فهمد.
  با سه جهشِ عمدی آزموده شد (حذفِ یک رویداد، افزودنِ یکی الکی، و ناپدید شدنِ
  خودِ `EV_DEPTH`) و هر سه قرمز دادند. جزئیاتش در `bot/README.md`.

  **نما RLS را دور می‌زند — این را با اندازه‌گیری فهمیدیم.** تا نما ساخته شد،
  با خودِ کلیدِ عمومیِ پنل رویش `curl` زده شد: `200` داد. یعنی نامِ مشتری، شماره‌ی
  تلفن و رفتارش با کلیدی که داخلِ جاواسکریپتِ پنل است خواندنی بود. علتش این است
  که نما با اختیارِ **مالکش** اجرا می‌شود و پیش‌فرضِ سوپابیس `select` را به `anon`
  هم می‌دهد. برای **جدول** این پیش نمی‌آید چون RLS پشتِ grant می‌ایستد؛ برای
  **نما** هیچ‌چیز پشتش نیست. بعد از `revoke`، همان `curl` شد `401`.
  (وارسیِ اینکه RLS واقعاً کار می‌کند هم با شواهد شد، نه با `[]`ِ خالی: جدولِ
  `products` سی‌ویک ردیف دارد و کلیدِ عمومی رویش `[]` می‌گیرد.)
  **هر نمای تازه‌ای که ساختی، دسترسی‌اش را در `db_invariants.expected.json` ثبت
  کن** — `server_audit.py` نمای ثبت‌نشده را قرمز می‌کند.

  ⚠️ **تصحیحِ همین جمله (۱۴۰۵/۰۵/۳۱): نما فقط وقتی RLS را دور می‌زند که
  `security_invoker` خاموش باشد.** این را موقعِ سنجیدنِ `customer_balances` فهمیدم
  و اولش شبیهِ نشتی به نظر رسید: کلیدِ عمومی رویش `200` می‌گیرد. ولی `[]`
  برمی‌گردانَد در حالی که نما **۲ ردیف دارد** — چون `security_invoker=true` است و
  RLSِ جدول‌های زیرین پشتش می‌ایستد. آن دو نمای دیگر این را ندارند، برای همین
  مجبور شدیم `revoke`شان کنیم.
  یعنی «`anon` گرانت دارد» نه اثباتِ نشتی است نه اثباتِ امنیت — **جفتش را باید
  دید**. و `[]`ِ خالی هم همچنان شاهد نیست؛ اینجا فقط با شمردنِ ردیف‌های واقعیِ
  نما معلوم شد که خالی‌بودن مالِ RLS است نه مالِ نبودِ داده.
  حالا `security_invoker` هم در snapshot ثبت می‌شود و بررسیِ تازه‌ای هر نمایی را
  که برای `anon` باز است **و** `security_invoker` ندارد قرمز می‌کند. با جهشِ عمدی
  آزموده شد (خاموش‌کردنش روی `customer_balances`) و گرفت.

  **آمار حق ندارد خرید را بخواباند.** `logEvent` هرگز `throw` نمی‌کند. یک جهشِ
  عمدی نشان داد اگر خطا بالا برود، سفارش ثبت می‌شود ولی **تأییدیه به مشتری
  نمی‌رسد** — بدترین حالت. نسخه‌ی اولِ بررسیِ شبیه‌ساز فقط وجودِ ردیف را می‌دید و
  همان جهش از زیرش در رفت؛ حالا رسیدنِ تأییدیه سنجیده می‌شود.
- **دو جورِ مرجوعی داریم و فرقشان مهم است** (با خواندنِ خودِ توابعِ دیتابیس روشن شد):
  - `return_invoice_items` (مرجوعی روی یک فاکتور): ردیفِ `invoice_items` را کم یا حذف
    می‌کند، `items_total` و `total_amount` فاکتور را دوباره حساب می‌کند، و یک ردیفِ
    `returns` با `invoice_id` پر و `settlement='invoice'` می‌سازد. **این مرجوعی همان
    لحظه از فروش کم شده.**
  - `create_return` (مرجوعیِ مستقل): فاکتور را دست نمی‌زند و `invoice_id`اش خالی است.
    **این یکی باید از فروش کم شود.**
  پنل این تفکیک را از اول درست رعایت می‌کند: `openReport` و `renderDashboard` با
  `invoice_id=is.null` فقط مرجوعیِ مستقل را می‌گیرند، `insReturns` هم برای نرخِ مرجوعی
  تعدادِ مرجوعیِ روی فاکتور را به فروشِ ناخالص برمی‌گرداند. **این‌ها را به‌هم نزن.**
  `dashboard_stats` عمداً مرجوعی را کم نمی‌کند؛ خودِ `renderDashboard` کم می‌کند.
  اگر آن تابع را «اصلاح» کنی، فروش دوبار کم می‌شود.

- **چرخه‌ی پول با داده‌ی واقعی تست شد** (۱۴۰۵/۰۵/۱۶): ده کالای نمونه وارد شد، یک سفارشِ
  تلگرام ساخته شد، با `convert_telegram_order` به فاکتورِ #۷۰۱۵ تبدیل شد (مشتری خودکار
  ساخته شد) و یک پرداختِ جزئیِ ۲٬۰۰۰٬۰۰۰ رویش نشست. ماندهٔ `customer_balances` با حسابِ
  دستی خواند: ۵٬۱۷۰٬۰۰۰ − ۲٬۰۰۰٬۰۰۰ = ۳٬۱۷۰٬۰۰۰ ✅ و سفارش به `done` رفت.
  **داده‌ی نمونه بعدش پاک شد** — دیتابیس تمیز است.
- **مسیرِ ویدیو تست شد** (۱۴۰۵/۰۵/۱۶) — با ساختنِ فیلمِ واقعیِ صدادار داخلِ خودِ مرورگر:
  فشرده‌سازی، نوارِ درصد، حفظِ صدا، آپلود، و صدا زدنِ `medPrime` همه کار می‌کنند. یک باگِ
  واقعی هم همان‌جا پیدا شد: مرورگر برای فیلمی که طولش در سرصفحه نیست `Infinity` می‌دهد
  و فشرده‌ساز با پیامِ «طولِ فیلم خونده نشد» رد می‌کرد؛ حالا با یک پرش به انتها طول را
  واقعی می‌گیرد. بررسیِ خودکارش در بازرس هست.
- **سافاری هم تست می‌شود:** `python3 auditor/audit.py panel/index.html -c auditor/audit.config.json -e webkit`
  همان موتورِ سافاری است، روی اندازه‌ی آیفون ۱۳. **هر تغییری که به رابط دست می‌زند را با
  هر دو موتور بزن** — یک باگِ واقعی فقط آنجا پیدا شد (Promiseی که با لغوِ View Transition
  رد می‌شد و کرومیوم نشانش نمی‌داد).
  اگر WebKit نصب نبود: `python3 -m playwright install webkit && python3 -m playwright install-deps webkit`
  (`install-deps` هم لازم است، نه فقط `install` — بدونِ آن باینری دانلود می‌شود ولی
  با نبودِ `libwebkit`/`libgstreamer` بالا نمی‌آید.)
  **روی وب‌کیت انتظار ۹۷ پاس + ۳ هشدار است، نه ۱۰۰.** آن سه، بررسی‌های `MediaRecorder`اند
  که وب‌کیتِ پلی‌رایت اصلاً این API را ندارد و خودِ بازرس ردشان می‌کند. ۹۷+۳ = همان ۱۰۰ِ
  کرومیوم. **این را «سه بررسیِ گم‌شده» نخوان.**
  با `-r` یکی بیشتر می‌شود، چون خودِ «هر N بررسی نتیجه‌ی یکسان داد» هم یک بررسی
  حساب می‌شود (کرومیوم ۹۰).
- **`MediaRecorder` روی آیفون وارسی شد** — نه با گوشی، با شبیه‌سازیِ محدودیتش.
  `MediaRecorder.isTypeSupported` طوری دستکاری شد که فقط mp4 قبول کند (همان کاری که
  سافاری می‌کند)، و `medRecMime` درست `video/mp4` را انتخاب کرد. حالتِ «فقط webm» و
  «اصلاً MediaRecorder ندارد» هم تست شد؛ دومی پیامِ روشن می‌دهد و می‌گوید فیلمِ زیرِ
  ۵۰ مگ بدونِ فشرده‌سازی بالا می‌رود. هر سه بررسیِ خودکار دارند.
  (WebKitِ پلی‌رایت اصلاً `MediaRecorder` ندارد، برای همین آن بررسی‌ها آنجا رد می‌شوند.)
- **پخشِ ویدیو در تلگرام — با شواهد حل شد، نه با حدس.** تلگرام فقط MPEG-4 را داخلِ چت
  پخش می‌کند و بقیه را به‌صورتِ «فایل» می‌فرستد. بایت‌های خروجیِ فشرده‌سازی خوانده شد:
  `00 00 00 24 66 74 79 70 69 73 6f 6d` یعنی جعبه‌ی `ftypisom` — **MP4ِ واقعی، با صدا.**
  بررسیِ خودکارش هست تا اگر روزی به webm برگشت، همان‌جا قرمز شود.
  اگر مرورگری mp4 نسازد، برنامه صریح هشدار می‌دهد که «تو تلگرام به‌صورتِ فایل می‌ره».
  تنها چیزی که هنوز چشمی دیده نشده، خودِ پخش روی گوشیِ مالک است — ولی ظرف و کدک درست است.
- **`ask()` گزینه‌ی امن را فوکوس می‌کند** (`autofocus` روی دکمه‌ی «بی‌خیال»). بدونِ آن،
  `showModal` اولین دکمه را فوکوس می‌کند و آن دکمه‌ی خطرناک است — یک Enter کافی بود.
  **این `autofocus` را برندار.**
- **سه ایرادِ نامرئی که با اندازه‌گیری پیدا شدند (۱۴۰۵/۰۵/۱۷)** — هیچ‌کدام خطا در
  کنسول نمی‌دادند، برای همین قبلاً از زیرِ دستِ بازرس در رفته بودند:
  ۱) هفت درخواستِ موازیِ صفحه‌ی بینش‌ها، هفت بار توکن را همزمان تازه می‌کردند (و در
  طوفانِ ۴۰۱، چهارده بار) — یعنی ریسکِ باطل شدنِ کلِ نشست وسطِ کار. حالا دقیقاً یک بار.
  ۲) بارِ اولِ باز شدنِ برنامه، سرویس‌ورکر که کنترل را می‌گرفت صفحه بی‌دلیل رفرش می‌شد.
  ۳) دکمه‌ی برگشتِ گوشی پنجره‌ها را نمی‌بست.
  هر سه حالا بررسیِ خودکار دارند (پنج بررسیِ تازه؛ جمع در **آن زمان** ۶۲ بررسی روی کرومیوم).
  **درسِ روشی:** ایرادی که خطا نمی‌دهد را فقط با شمردن می‌شود دید — قبل و بعدِ هر
  «درست کردم» را اندازه بگیر، وگرنه نمی‌دانی چیزی عوض شده یا نه.
- **مسابقه‌ی پاسخ‌های شبکه (۱۴۰۵/۰۵/۱۷)** — با کند کردنِ عمدیِ یک پاسخ در تست ثابت شد:
  کاربر تایپ می‌کرد، بعد کادر را پاک می‌کرد، و نتیجه‌ی جست‌وجو روی لیستِ کامل می‌نشست.
  با `seqStart`/`seqStale` درست شد (بخشِ ۵ را ببین).
- **کدِ مرده‌ی `× ۰` پاک شد.** سه جا (`cst`/`cT`/`cM` در داشبورد، `rc` در بینش‌ها،
  `retCost` در گزارش) بازمانده‌ی محاسبه‌ی سود بودند که با حذفِ `cost_price` بی‌معنی شده
  بود؛ همیشه صفر بودند و هیچ‌جا خوانده نمی‌شدند. **اگر روزی قیمتِ خرید برگشت، این‌ها را
  از نو بنویس — نسخه‌ی قبلی را احیا نکن، چون فقط اسکلتِ خالی بود.**
- **چیزی که آزمایش شد و ایراد نبود:** پنج جای دانلود (`revokeObjectURL` بلافاصله بعد از
  `a.click()`) مشکوک بود، ولی روی هر دو موتور فایل کاملِ ۲۴ کیلوبایتی سالم دانلود شد.
  فقط با `dlBlob` ناهماهنگ‌اند، خراب نیستند. **وقتت را سرِ این تلف نکن.**

> **موقعِ وارسیِ سایتِ زنده، مسیرِ `/` را با پارامترِ تصادفی بگیر.** یک بار بعد از یک
> انتشارِ کاملاً موفق، `curl` روی `/` محتوای **قدیمی** برگرداند (`cf-cache-status: HIT`)
> و نزدیک بود انتشار را خراب اعلام کنم. با `?cachebust=…` همان فایل بایت‌به‌بایت با
> مخزن یکی بود. `sw.js` کش نمی‌شود، ولی `/` چند لحظه از کشِ لبه می‌آید.

## ۷.۵ وضعیتِ سمتِ سرور (وارسی‌شده در ۱۴۰۵/۰۵/۱۶)

کلِ دیتابیس و رباتِ تلگرام یک بار خوانده و وارسی شد. این‌ها را دوباره از صفر بررسی نکن:

- **ثبت‌نام بسته است** (`disable_signup: true`) و فقط یک کاربر وجود دارد. این مهم است:
  سیاست‌های RLS همه از نوعِ «هر کاربرِ واردشده، همه‌چیز» هستند و امنیتشان **تماماً** به
  بسته بودنِ ثبت‌نام بند است. اگر روزی بازش کردی، اول سیاست‌ها را محدود کن.
- **«Leaked Password Protection» روی پلنِ رایگان نیست — دنبالش نفرست.** بازرسِ امنیتیِ
  سوپابیس هشدارِ `auth_leaked_password_protection` را **همیشه** می‌دهد و این هشدار
  اینجا بسته نمی‌شود. مالک کلید را زد و سوپابیس صریح رد کرد:
  «Configuring leaked password protection via HaveIBeenPwned.org is available on
  Pro Plans and up» (سنجیده شد ۱۴۰۵/۰۵/۲۲). **این هشدار را باگ نخوان و مالک را
  دوباره پشتِ آن دکمه نفرست** — تنها راهش ارتقا به پلنِ Pro است.
  ضمناً سودش اینجا کم بود: فقط هنگامِ **ساختن یا عوض‌کردنِ** رمز کار می‌کند، ثبت‌نام
  بسته است و یک کاربر بیشتر نیست، یعنی رمزِ فعلی را هم وارسی نمی‌کرد.
  جایگزینِ رایگان در همان صفحه‌ی `Authentication → Sign In / Providers → Email`:
  `Minimum password length` و `Password requirements`. **مالک هر دو را در ۱۴۰۵/۰۵/۲۲
  تنظیم کرد** (قبلش طول روی ۶ بود و شرطِ کاراکتر اصلاً انتخاب نشده بود).
  ⚠️ این یکی **گفته‌ی مالک است، نه اندازه‌گیریِ من** — هیچ ابزاری که در دسترسِ ما
  باشد تنظیماتِ auth را نمی‌خوانَد، و راهِ آزمایشش دست‌زدن به رمزِ حسابِ واقعیِ
  اوست. اگر روزی لازم شد قطعی بدانی، از خودش بخواه صفحه را نشان بدهد.
  و مهم‌تر از هر دو، **MFA روی خودِ حسابِ supabase.comِ مالک** — رایگان است
  (`/dashboard/account/security`) و از کلیدِ service_role و کلِ داده محافظت می‌کند،
  نه فقط از ورود به پنل. **مالک گفته MFA روی خودِ پنل را نمی‌خواهد — پیشنهادش نکن.**
- `customer_balances` دقیقاً همان فرمولِ بخشِ ۶ است. همه‌ی CHECK constraintها سرِ جایشان‌اند.
- `create_invoice` پرداختِ سرِ فروش را در جدولِ `payments` **نمی‌نویسد**، پس جمع‌زدنِ
  `paid_amount` با ردیف‌های `payments` دوباره‌کاری نیست.
- `convert_telegram_order` تخفیفِ کدِ ربات را درست به `discount` فاکتور تبدیل می‌کند،
  سقفش را کنترل می‌کند و یک سفارش را دوبار فاکتور نمی‌کند.
- `paid_confirmed_at` را فقط ادمینِ ربات می‌زند؛ مشتری نمی‌تواند پرداختِ خودش را تأیید کند.
- **چهار `chat_id` در `bot_admins` هست و هر چهارتا خودِ مالک است** (تأییدِ خودش).
  دنبالِ نفوذی نگرد. مدیرِ ربات می‌تواند پرداخت تأیید کند و با `/پشتیبان` بکاپِ کامل بگیرد.
- پنج فاکتورِ آزمایشیِ قدیمی همه `cancelled`اند، پس گزارش‌ها از صفر شروع می‌شوند.
- انجام شد: `search_path` تابعِ `block_payment_on_cancelled_invoice` ثابت شد (تریگر بعدش
  تست شد و هنوز پرداخت روی فاکتورِ باطل را رد می‌کند)، و تابعِ رهاشده‌ی `nettest` بی‌اثر شد.

**انجام شد — کلیدِ cron دیگر داخلِ کد نیست.** تابعِ `cronKey()` اول `CRON_KEY` را از
Secrets می‌خواند و اگر نبود از جدولِ قفل‌شده‌ی `app_config` (کلیدِ `cron_key`) برمی‌دارد.
دستورِ `cron.job` (جمعه‌ها ۲۰:۰۰ تهران) با همان کلید به‌روز شد. اگر کلید را عوض کردی،
**هم `app_config` هم `cron.job` را با هم عوض کن** وگرنه گزارشِ هفتگی و بکاپ می‌خوابد.

**`/claimadmin` حذف شد** (نسخه‌ی ۳۲ ربات). آن دستور اگر روزی `bot_admins` خالی می‌شد،
هر غریبه‌ای را مدیر می‌کرد. حالا افزودنِ مدیر فقط با `insert` مستقیم در دیتابیس است.

> نکته‌ی روشی: ابزارِ استقرارِ edge function کلِ فایل را می‌خواهد و آن فایل ۳۹۸۹ نویسه‌ی
> غیرِ‌انگلیسی و ۵۸ نیم‌فاصله‌ی نامرئی دارد. **بازتایپ نکن** — نسخه‌ی مستقر را با
> `get_edge_function` بگیر، همان متن را برنامه‌ای ویرایش کن، مستقر کن، و بعد از استقرار
> دوباره بگیر و sha256 را با فایلِ محلی مقابله کن. دو بار همین‌طور انجام شد و بایت‌به‌بایت
> یکی درآمد.

## ۸. چیزهایی که پیشنهاد شد و مالک ردشان کرد

**این‌ها کارِ معلق نیستند.** پرسیده شدند و جواب «نه» بود (۱۴۰۵/۰۵/۲۳).
**دوباره پیشنهادشان نکن.**

- **کار کردن با عکسی که مشتری می‌فرستد.** امروز ربات فقط در دو حالت عکس را
  می‌گیرد: مرحله‌ی «فیش بفرست» و مرحله‌ی «سؤال دارم». مشتریِ عادی که همین‌طوری
  عکس بفرستد **هیچ جوابی نمی‌گیرد** — این را بدان تا «باگ» گزارشش نکنی.
  سه راه مطرح شد و مالک هر سه را رد کرد: جست‌وجوی تصویریِ واقعی (هزینه‌ی ماهانه
  دارد و روی پوشاکِ عمده‌ی شبیهِ هم دقتش پایین است)، شکستنِ سکوت با یک پیامِ
  راهنما، و فرستادنِ خودکارِ عکس برای مالک.
- **گزارشِ سود بر اساسِ قیمتِ خرید.** `cost_price` **عمداً** در پنل نیست، پس این
  گزارش اصلاً موضوعیت ندارد. بازمانده‌های محاسبه‌ی سود قبلاً پاک شده‌اند (بخشِ ۷).

## ۹. لحن

کاربر مستقیم و صمیمی حرف می‌زند و «حاجی» صدا می‌کند. کوتاه و روشن جواب بده،
بدونِ تعارف. وقتی اشتباه کردی صریح بگو. وقتی چیزی را تست نکرده‌ای، بگو تست نکرده‌ام —
حدس را به‌جای شواهد جا نزن.
