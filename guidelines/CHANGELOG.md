# تغییراتِ دستورالعمل

قالبِ نسخه: `YYYY.MM.N` — سال، ماهِ میلادی، شماره‌ی ترتیبیِ همان ماه.
هر نسخه‌ای که کدِ موجود را نامنطبق کند، باید ورودیِ متناظر در `MIGRATIONS.md` داشته باشد (SELF-02).

<!-- ورودیِ تازه بالای همین خط اضافه شود -->

## 2026.08.9 — 2026-08-14

یک روز، سه بار، یک الگو: ابزار گفت «سبز» در حالی که چیزی را نسنجیده بود.

**افزوده شد**
- `CORE-12` — بررسی‌ای که نتوانست اجرا شود، پاس نشده است.
- `TEST-10` — شبیه‌ساز منطق را ثابت می‌کند، نه خاصیتِ زمانِ اجرا.

**چرا:** هر دو از خرابیِ اندازه‌گیری‌شده درآمدند، نه از حدس.

۱. برای مسیرِ بازِ ویدیوی ربات یک سقفِ نرخِ درون‌حافظه‌ای نوشته شد. شبیه‌ساز
   بی‌عیب سبز داد. روی تولید ۶۴۰ درخواستِ موازی و بعد ۷۰۰ درخواستِ پشتِ‌هم زده
   شد: **هیچ ۴۲۹ای**. یک کاوشگرِ موقت که شناسه‌ی تصادفیِ لحظه‌ی بارگذاریِ ماژول
   را برمی‌گرداند علتش را گفت — ۱۴ درخواست، ۱۴ ایزوله‌ی متفاوت، شمارنده همیشه
   صفر. در آن محیط هیچ حالتی بینِ درخواست‌ها زنده نمی‌مانَد، پس آن محافظ
   **ساختاراً** نمی‌توانست کار کند. شبیه‌ساز یک پردازه است و این فرض را رایگان
   برآورده می‌کرد. → `TEST-10`

۲. یک جهشِ سلامت (که وارسی می‌کرد محافظِ احرازِ یک مسیر هنوز سرِ جایش است) با
   اضافه شدنِ یک مسیرِ تازه دو تطابق پیدا کرد، ابزار از اعمالش صرف‌نظر کرد، یک
   هشدار چاپ کرد و **کدِ خروجیِ ۰ داد**. دروازه سبز ماند در حالی که دو محافظ را
   سنجیده بود نه سه‌تا.

۳. نُه بررسیِ ثابت‌های دیتابیس فقط با یک سوییچِ دستی اجرا می‌شدند و بدونِ آن
   بی‌صدا رد می‌شدند — یعنی عملاً هیچ‌وقت اجرا نشده بودند.

   دوم و سوم یک چیزند و `CORE-12` را ساختند.

**نکته‌ی روشی:** هر سه از خواندنِ لاگِ یک اجرای **سبز** پیدا شدند، نه از یک
قرمزی. علامتِ سبز شاهد نیست؛ لاگ شاهد است.

## 2026.08.8 — 2026-08-13

دومین قاعده‌ای که از کاربردِ واقعی درآمد — و این بار من اولش گفتم لازم نیست.

**افزوده شد**
- `API-07` — در مرز رد کن، ترمیم نکن؛ و یک تابعِ خواندن برای دو قرارداد ننویس.

**چرا:** مالک با همین دستورالعمل یک ابزارِ ثبتِ خرجِ تک‌فایل ساخت (فارسی، RTL،
دو تم، تقویمِ جلالیِ دست‌نویس، ذخیره‌ی محلی). با اندازه‌گیری تقریباً همه‌چیز پاس
شد: تقویم روی ۲۵٬۹۳۳ روز مقابلِ `Intl` صفر اختلاف، کنتراست و هدفِ لمسی صفر خطا
در دو تم و دو عرض و دو موتور، حلقه‌ی فوکوس روی هر ۱۶ توقفِ Tab، تله‌ی فوکوس و
Esc در پنجره‌ی تأیید، صفر تزریق، CSV با محافظِ فرمولِ اکسل، و `UI-11` هم رعایت
شده بود. یک باگ داشت و باگش **پول** بود:

فایلِ پشتیبانی که مبلغش `12.5` بود، `125` ذخیره می‌شد و `850.75` می‌شد `85075`.
برنامه هیچ خطایی نمی‌داد و آن ردیف‌ها را «سالم» می‌شمرد — «۵ ردیف اضافه شد».

علتش یک تابع بود که دو قرارداد را سرویس می‌داد. `parseNum` برای تایپِ آدم نوشته
شده بود، جایی که `.` جداکننده‌ی هزار است (`۸۵۰.۰۰۰` یعنی هشتصدوپنجاه هزار). همان
تابع روی عددِ JSON هم اجرا می‌شد، جایی که `.` ممیز است.

**چرا اول گفتم قاعده لازم نیست و بعد نظرم عوض شد:** فکر کردم `DATA-03` (پول عددِ
صحیح) و `TEST-06` کافی‌اند. ولی وقتی نشستم اصلاحش کنم معلوم شد هیچ‌کدام این را
نمی‌گیرند: کد **اعتبارسنجی داشت** (`validRow`) و خروجی‌اش هم **عددِ صحیح بود** —
فقط عددِ صحیحِ غلط. چیزی که نبود این است: اعتبارسنجی‌ای که مقدار را عوض می‌کند
اعتبارسنجی نیست. `API-01` می‌گوید در مرز اعتبارسنجی کن، ولی نمی‌گوید **رد کن،
ترمیم نکن**.

**نسخه‌ی ماشین‌خوان:** `stack.json` هم به ۲۰۲۶.۰۸.۸ رفت. نسخه‌ی بسته‌ها عوض نشد.

## 2026.08.7 — 2026-08-09

اولین قاعده‌ای که از **کاربردِ واقعی** درآمد، نه از بازبینیِ خودمان.

**افزوده شد**
- `UI-11` — محتوا قبل از جلوه وجود دارد؛ جلوه رویش می‌نشیند.

**چرا:** مالک با همین دستورالعمل یک سایتِ تک‌فایل ساخت (فراری F40، فارسی، RTL،
دو تم) و آن را برای بازبینی داد. سایت تقریباً همه‌ی قاعده‌های §۹ را پاس داد —
کنتراست در هر دو تم صفر خطا، هدفِ لمسی صفر خطا، حلقه‌ی فوکوس روی هر ۱۴ توقفِ Tab،
کشو با تله‌ی فوکوس و بازگشتِ فوکوس با Esc، فقط ویژگیِ منطقی، نردبانِ نام‌دار،
کدِ خودِ سایت ۱۹٫۷ کیلوبایتِ فشرده. ولی یک چیز از زیرِ همه‌ی قاعده‌ها رد شد:

عددهای اصلیِ صفحه (`۴۷۸ اسب بخار`، `۱۱۰۰ کیلوگرم`، `۳۲۴ کیلومتر`، `۱۳۱۱ دستگاه`)
در نشانه‌گذاری **`۰`** نوشته شده بودند و مقدارِ درست فقط در `data-count` زندگی
می‌کرد. با JS خاموش، صفحه اینها را نشان می‌داد: چپ‌به‌راست (چون `dir`/`lang` هم با
اسکریپت نشانده می‌شدند)، چهار کارتِ `۰`، و بقیه‌ی صفحه سفید — چون `.reveal`
با `opacity:0` شروع می‌شود و کلاسِ `in` را فقط `IntersectionObserver` می‌دهد.

هیچ قاعده‌ای این را نمی‌گرفت. `UI-07` حالت‌های ناهمگام را می‌پوشاند، ولی این
ناهمگام نیست — داده از اول آنجاست، فقط در جایی گذاشته شده که بدونِ اسکریپت
خوانده نمی‌شود. تفاوتِ مهم این است که حالتِ اولیه **ناقص** نبود، **غلط** بود:
«۰ اسب بخار» یک ادعای اشتباه است که هیچ خطایی هم نمی‌دهد.

**نسخه‌ی ماشین‌خوان:** `stack.json` هم به ۲۰۲۶.۰۸.۷ رفت. نسخه‌ی بسته‌ها عوض نشد.

## 2026.08.6 — 2026-08-08

راهِ «اسکریپتِ راه‌اندازیِ محیط» برداشته شد.

**حذف شد**
- `install.py --if-missing` و بخشِ «هیچ‌وقت نگو نصبش کن» در `README.md`.

**چرا:** آن گزینه فقط برای یک سناریو ساخته شده بود — گذاشتنِ فرمانِ نصب در
اسکریپتِ راه‌اندازیِ محیطِ Claude Code. ولی مستندِ رسمی می‌گوید آن اسکریپت
**فقط** از پنجره‌ی انتخابگرِ محیط در claude.ai/code تنظیم می‌شود و «هیچ صفحه‌ی
تنظیمات یا نشانیِ مستقیمی ندارد» — یعنی نه API دارد نه از داخلِ نشست دسترسی‌پذیر
است. مالک هم تصمیم گرفت آن مسیر را نرود.

قابلیتی که تنها مسیرِ استفاده‌اش کنار گذاشته شده، **کدِ مرده** است و مستندش
کاربر را به راهی می‌فرستد که قرار نیست برود. پس برداشته شد.

**روالِ نصب برمی‌گردد به همان چیزی که بود:** کاربر می‌گوید «دستورالعمل را روی
این پروژه نصب کن» و `install.py` یک بار اجرا می‌شود.

**نسخه عقب نرفت** (۰۸.۵ → ۰۸.۴ نشد) چون `gl-update` نسخه‌ها را عددی مقایسه
می‌کند و کپی‌ای که روی ۰۸.۵ باشد هیچ‌وقت ۰۸.۴ را تازه‌تر نمی‌بیند — عقب بردنِ
نسخه یعنی به‌روزرسانیِ خاموش.

## 2026.08.5 — 2026-08-08

نصب می‌تواند در هر شروعِ نشست اجرا شود.

**افزوده شد**
- `install.py --if-missing` — اگر بسته از قبل نصب باشد، **بدونِ یک بایت شبکه**
  فوراً بیرون می‌آید (اندازه‌گیری شد: ۷۴ میلی‌ثانیه). این همان چیزی است که
  می‌گذارد یک خط در اسکریپتِ راه‌اندازیِ محیط بگذاری و دیگر هیچ‌وقت «نصبش کن»
  نگویی: هر پروژه‌ی تازه خودش نصب می‌شود، هر پروژه‌ی نصب‌شده رد می‌شود.

## 2026.08.4 — 2026-08-08

پروژه‌ی تازه‌نصب حالا `CLAUDE.md` هم دارد.

**افزوده شد**
- `install.py` حالا `CLAUDE.md` می‌سازد که `@guidelines/FULLSTACK.md` را import می‌کند،
  به‌علاوه‌ی ترتیبِ اولویت و دروازه‌ی وارسیِ همان پروژه.
  قبلاً فقط هوکِ شروعِ نشست می‌گفت «این فایل را بخوان» — کار می‌کرد، ولی یک لایه
  ضعیف‌تر از بار شدنِ واقعیِ فایل در حافظه بود.
- فایلِ موجود **هیچ‌وقت رونویسی نمی‌شود**: یک بلوکِ نشانه‌دار به آن اضافه می‌شود و
  اجرای دوباره چیزی تکرار نمی‌کند.
- `CLAUDE.md` به `never_touch` اضافه شد — مالِ خودِ پروژه است و از منبع نمی‌آید.

## 2026.08.3 — 2026-08-08

نصب دیگر با بسته‌ی نیم‌کهنه تمام نمی‌شود.

**درست شد**
- `install.py` بعد از گرفتنِ بسته، یکدستی‌اش را می‌سنجد: نسخه‌ای که `FULLSTACK.md`
  ادعا می‌کند باید با `stack.json` بخواند. اگر نخواند، تا سه بار دوباره می‌گیرد و
  آخرش پیامِ روشن می‌دهد به‌جای قرمزیِ گیج‌کننده‌ی `self-check`.
  **این با فرمانِ واقعیِ نصب پیدا شد، نه با خواندنِ کد:** `raw.githubusercontent.com`
  هر فایل را جدا کش می‌کند، پس درست بعد از یک پوش می‌شود نصفِ بسته را تازه گرفت و
  نصفش را کهنه.
- تاریخِ سرصفحه اصلاح شد: «۹ مرداد ۱۴۰۵» با «2026-08-09» نمی‌خواند
  (۱۴۰۵/۰۵/۱۷ = 2026-08-08).

## 2026.08.2 — 2026-08-09

کپی‌های این بسته دیگر جدا کهنه نمی‌شوند.

**افزوده شد**
- `SELF-05` — یک منبعِ رسمی، جریان یک‌طرفه. کپیِ محلیِ ویرایش‌شده از به‌روزرسانی جا می‌مانَد.
- `MIG-07` — گرفتنِ نسخه‌ی تازه مهاجرت نیست؛ `lock.json` دست نمی‌خورد تا فاصله آشکار بماند.
- `guidelines/gl-update.py` — سنجش و گرفتنِ نسخه از منبع. سه محافظ: `lock.json`
  هیچ‌وقت از منبع نمی‌آید، فایلِ دست‌کاری‌شده بدونِ `--force` رونویسی نمی‌شود، و کدِ
  اجرایی جز با `--with-tools` نمی‌آید.
- `guidelines/source.json` — نشانیِ منبع و فهرستِ بسته. `is_origin` روی `auto` است و
  از روی remoteهای گیت تشخیص می‌دهد اینجا منبع است یا کپی — **هیچ تنظیمِ دستی‌ای لازم نیست**.
- `guidelines/install.py` — نصبِ کاملِ بسته روی یک پروژه با یک فرمان: گرفتنِ فایل‌ها،
  ساختِ `lock.json` با دروازه‌ی وارسیِ حدس‌زده‌شده از خودِ پروژه، **ادغامِ** هوک در
  `.claude/settings.json` بدونِ خراب کردنِ تنظیماتِ موجود، گذاشتنِ گردش‌کارِ هفتگی،
  و به‌روزرسانیِ `.gitignore`.
- محافظِ تازه: اگر فایل‌های بسته کامیت‌نشده عوض شده باشند، `--apply` می‌ایستد.
- `guidelines/templates/guideline-upstream.yml` — قالبِ گردش‌کارِ هفتگی برای پروژه‌های دیگر.
- `/gl-pull` — گرفتن از منبع، با اجازه‌گرفتن قبل از آوردنِ کدِ اجرایی.
- هوکِ شروعِ نشست حالا خبرِ «منبع جلوتر است» را هم می‌دهد — **بدونِ انتظارِ شبکه**:
  کش را می‌خواند و اگر کهنه بود در پس‌زمینه تازه‌اش می‌کند (اندازه‌گیری شد: ۵۰ تا ۹۰
  میلی‌ثانیه، حتی با شبکه‌ی قطع).

**عوض شد**
- §۱۳ حالا بخشِ «به‌روز ماندنِ کپی‌ها» را دارد.

**مهاجرت:** دارد — `MIGRATIONS.md`. این یکی **فقط این بار** دستی است، چون پروژه‌ای
که `gl-update.py` ندارد نمی‌تواند نسخه‌ای را بگیرد که خودِ آن فایل در آن است.

## 2026.08.1 — 2026-08-08

نسخه‌ی نخست.

**افزوده شد**
- ۱۰۷ قاعده با شناسه‌ی پایدار در ۱۵ بخش (`ACT`, `PREC`, `CORE`, `DOD`, `STACK`,
  `ARCH`, `DATA`, `API`, `SEC`, `UI`, `TEST`, `OPS`, `GIT`, `SELF`, `MIG`).
- بلوکِ فعال‌سازیِ خودکار (§۰) تا فایل چه از `CLAUDE.md` و چه از آپلود در گفت‌وگو
  بدونِ هیچ مقدمه‌ای فعال شود.
- ترتیبِ اولویتِ صریح (§۱) — قاعده‌های پروژه بالاتر از این دستورالعمل می‌نشینند.
- `stack.json`: نسخه‌های پین‌شده به‌عنوانِ منبعِ حقیقتِ ماشین‌خوان.
- `check-drift.py`: سنجشِ نسخه‌ها با npm / PyPI / nodejs.org / endoflife.date.
- `sync-table.py`: جدولِ §۴ از `stack.json` ساخته می‌شود، دستی نوشته نمی‌شود.
- `lock.json`: مُهرِ انطباق + دروازه‌ی وارسی + استثناها.
- `MIGRATIONS.md` و پروتکلِ مهاجرت (§۱۴).
- `self-check.py`: وارسیِ سازگاریِ درونیِ خودِ این مجموعه.
- هوکِ `SessionStart` برای اعلامِ خودکارِ نسخه و مهاجرتِ معلق.
- دستورهای `/gl-check`، `/gl-migrate`، `/gl-sync`.
- گردش‌کارِ هفتگیِ سنجشِ نسخه‌ها.

**نسخه‌های پایه (سنجیده‌شده در 2026-08-08)**
TypeScript 7.0.2 · Node 24.19.0 LTS · pnpm 11.20.0 · Vite 8.2.1 · Next 16.3.0 ·
React 19.2.8 · Tailwind 4.3.3 · Biome 2.5.7 · ESLint 10.8.1 · Vitest 4.1.10 ·
Playwright 1.62.1 · Zod 4.4.3 · PostgreSQL 18 · Drizzle 0.45.2 · Hono 4.13.1 ·
TanStack Query 5.101.4 · OpenTelemetry 1.9.1 · Ruff 0.16.2 · uv 0.12.3

**استثناهای ثبت‌شده برای همین مخزن**
`panel-no-framework`، `panel-inline-handlers`، `panel-anon-key-client` — دلیل‌ها در `lock.json`.
