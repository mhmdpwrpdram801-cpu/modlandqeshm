#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""جانگهدارِ دروازه‌ی پروژه — عمداً همیشه قرمز است.

نصبِ تازه‌ی دستورالعمل قبلاً از همان اول سبز بود: تنها دروازه‌اش `self-check.py`
بود که فقط می‌گوید «دستورالعمل با خودش می‌خواند»، نه اینکه پروژه‌ی تو کار می‌کند.
یعنی هر پروژه‌ی تازه یک **سبزِ توخالی** تحویل می‌گرفت — دقیقاً همان چیزی که
`CORE-12` ممنوعش می‌کند.

پس تا وقتی دروازه‌ی واقعیِ این پروژه تعریف نشده، اینجا قرمز می‌ماند. سبز شدن
باید یعنی «چیزی سنجیده شد»، نه «چیزی برای سنجیدن پیدا نشد».

**چطور خاموشش کنی:** در `guidelines/lock.json` → `verify` این ورودی را با
فرمانِ تستِ خودت عوض کن. مثلاً:

    {"name": "تستِ برنامه", "cmd": "npm test", "note": "دروازه‌ی اصلی"}

اگر پروژه واقعاً چیزی برای سنجیدن ندارد (مثلاً فقط سند است)، ورودی را حذف کن و
دلیلش را در `waivers` بنویس — تصمیمِ آگاهانه اشکالی ندارد، سکوت اشکال دارد.
"""

import os
import sys

MSG = """
  ❌ دروازه‌ی این پروژه هنوز تعریف نشده.

  تنها چیزی که الان سنجیده می‌شود سازگاریِ درونیِ خودِ دستورالعمل است —
  که راجع به کدِ تو هیچ نمی‌گوید. سبز دادن در این حالت یعنی دروغ (CORE-12).

  برای رفعش، در guidelines/lock.json → verify این ورودی را با فرمانِ
  تستِ خودت عوض کن:

      {"name": "تستِ برنامه", "cmd": "npm test", "note": "دروازه‌ی اصلی"}

  اگر این پروژه واقعاً چیزی برای سنجیدن ندارد، ورودی را بردار و دلیلش را
  در waivers بنویس. تصمیمِ آگاهانه اشکالی ندارد؛ سکوت اشکال دارد.
"""


def main() -> int:
    # یک راهِ فرار برای وقتی که کسی عمداً می‌خواهد بدونِ دروازه جلو برود.
    # صریح است و در تاریخچه‌ی فرمان دیده می‌شود — بر خلافِ رد شدنِ بی‌صدا.
    if os.environ.get("GL_NO_PROJECT_GATE") == "1":
        print("  ⚠️  دروازه‌ی پروژه با GL_NO_PROJECT_GATE=1 دور زده شد — این موقتی باشد.")
        return 0
    print(MSG)
    return 1


if __name__ == "__main__":
    sys.exit(main())
