# نمونه‌ی `GIT-01` — در زمانِ اجرا ساخته می‌شود

این مورد **مخزنِ گیتِ واقعی** لازم دارد و مخزنِ تودرتو در گیت کامیت نمی‌شود.
پس `run.py` هر دو نمونه را در پوشه‌ی موقت می‌سازد و بعدش پاک می‌کند:

  `bad/`  — روی `main`، با تغییرِ کامیت‌نشده  → باید گرفته شود
  `good/` — روی `feature/x`، با **همان** تغییرِ کامیت‌نشده → باید ساکت بماند

تنها فرقِ دو نمونه **نامِ شاخه** است. اگر نمونه‌ی سالم را تمیز می‌ساختیم، ساکت
ماندنش می‌توانست فقط به‌خاطرِ نبودِ تغییر باشد و درباره‌ی خودِ قاعده هیچ چیزی
ثابت نمی‌کرد (`TEST-12`).
